# 実装タスクリスト

`spec.md` を細分化した作業単位。上から順に消化する。

## 進め方

- **1PR単位 = 1つの振る舞い**（🔴テスト + 🟢実装の対）。`main` から `feat/xxx` ブランチを切る
- **PRの下限は 🔴+🟢 の対**。🔴 だけで出すとテストが失敗した状態になり CI が赤でマージできない
- TDD を厳守する。🔴 は**失敗を確認してから** 🟢 に進む
- PR を作ったら CI 全緑を確認し、レビュー待ちで止める（マージはユーザーが行う）
- マージ後にバグが出たら Issue を立て、修正PRで `Closes #N` する

## 記号

| 記号 | 意味 |
| --- | --- |
| 🔴 | 失敗するテストを書く（Red） |
| 🟢 | テストを通す実装（Green） |
| 🔧 | 設定・雑務 |

---

## 完了済み

<details>
<summary><b>フェーズ0: 環境構築</b> — PR #1 ✅（13タスク）</summary>

`docker compose up` で Go が `/health` に200を返すところまで。
Go / Docker Desktop / make の導入、echo サーバ、Dockerfile（dev: air / prod: distroless）、
Vite スキャフォールド、compose、Makefile、CI 3ジョブ、.golangci.yml。

</details>

<details>
<summary><b>フェーズ1: ドメイン層 + 献立マスタ</b> — PR #2 ✅（39タスク）</summary>

repository の統合テストが緑。
ドメイン型5つ（カバレッジ100%）、マイグレーション（embed）、pgx接続プール（リトライ付き）、
献立マスタ120件（冪等シード）、MenuRepository（統合テスト12件）、service/ports.go。

</details>

---

## フェーズ2: 献立検索（1食分）

> 完了条件: 絞り込み・候補枯渇の単体テストが緑

### PR 2-A: RFC 7807 エラーレスポンス `feat/problem-json` ✅

他のPRが全てこの形式でエラーを返すため最初に入れる。

- [x] 🔴 `Problem` 型のテスト: 各フィールドがJSONに出る
- [x] 🔴 テスト: Content-Type が `application/problem+json`
- [x] 🔴 テスト: ドメインのエラーが対応するHTTPステータスに変換される
- [x] 🔴 テスト: 未知のエラーは500になり、詳細を外部に漏らさない
- [x] 🟢 `Problem` 型と echo のカスタムエラーハンドラを実装
- [x] 🔧 `main.go` に結線

### PR 2-B: 乱数源の抽象化 `feat/random-source` ✅

`SuggestMenu` のテストを決定的にするため、実装より先に入れる。

- [x] 🔴 テスト: 決定的な乱数源が指定した値を返す
- [x] 🔴 テスト: 候補が空のとき `Pick` がエラーになる
- [x] 🔴 テスト: 候補1件なら必ずそれを返す
- [x] 🟢 `Randomizer` インターフェースと実装（crypto/rand ベース）
- [x] 🟢 テスト用の決定的な実装

### PR 2-C: SuggestMenu の絞り込み `feat/suggest-menu-filter` ✅

- [x] 🔧 fake の `MenuRepository` を作る
- [x] 🔴 テスト: genre 指定で該当ジャンルのみ候補になる
- [x] 🔴 テスト: difficulty 指定で該当難易度のみ候補になる
- [x] 🔴 テスト: 両方指定で両方に合うもののみ
- [x] 🔴 テスト: 両方 nil で全件が候補
- [x] 🔴 テスト: 不正な genre は `ErrInvalidGenre`
- [x] 🟢 `service.SuggestMenu` を実装

### PR 2-D: SuggestMenu の候補枯渇 `feat/suggest-menu-exhausted` ✅

- [x] 🔴 テスト: 候補0件で `ErrNoMenuFound`
- [x] 🔴 テスト: 候補1件ならそれが返る（境界値）
- [x] 🔴 テスト: repository のエラーがラップされて返る
- [x] 🔴 テスト: `ErrNoMenuFound` と repository のエラーが区別できる
- [x] 🟢 エラーハンドリングを実装

### PR 2-E: SuggestMenu の除外指定 `feat/suggest-menu-exclude` ✅

- [x] 🔴 テスト: `ExcludeIDs` の献立が候補から外れる
- [x] 🔴 テスト: 全件除外すると `ErrNoMenuFound`
- [x] 🟢 除外を実装（フェーズ6の履歴除外で使う）
      → **実装は不要だった**。除外は PR #2 で repository が実装済み
      （`AND NOT (id = ANY($3::uuid[]))`、統合テスト3件）で、SuggestMenu は
      filter をそのまま渡すため既に動いていた。テストのみ追加して契約を固定した。

### PR 2-F: `GET /menus/suggest` `feat/api-suggest-menu` ✅

- [x] 🔴 テスト: 200 とJSON構造（id / name / genre / difficulty / description）
- [x] 🔴 テスト: `?genre=japanese` が service に渡る
- [x] 🔴 テスト: `?difficulty=easy` が service に渡る
- [x] 🔴 テスト: クエリ無しで両方 nil が渡る
- [x] 🔴 テスト: 不正な genre で 400
- [x] 🔴 テスト: 不正な difficulty で 400
- [x] 🔴 テスト: 候補0件で 422
- [x] 🟢 `MenuHandler.Suggest` を実装
- [x] 🔧 `/api/v1` にルーティング（`MenuHandler.RegisterRoutes`。main.go への結線は PR 2-H）

### PR 2-G: `GET /menus/:id` `feat/api-get-menu` ✅

- [x] 🔴 テスト: 200 と献立の詳細
- [x] 🔴 テスト: 存在しないIDで 404
- [x] 🔴 テスト: 不正なUUIDで 400（ゼロ値UUIDを含む）
- [x] 🔴 テスト: `/menus/suggest` が `:id` に飲み込まれない
- [x] 🟢 `MenuHandler.Get` を実装
- [x] 🟢 `service.GetMenu` を実装（repository の `FindByID` は PR #2 で実装済み）

### PR 2-H: 実機結線 `feat/wire-menu-api` ✅

- [x] 🔧 `main.go` で pool / repository / service / handler を組み立て
- [x] 🔧 DBに繋がらない場合の起動時エラー（`DATABASE_URL` 未設定・接続不可とも exit 1）
- [x] 🔧 実機確認: `curl "localhost:8080/api/v1/menus/suggest?genre=japanese&difficulty=easy"`
- [x] 🔧 実機確認: 120件のマスタから実際に献立が返ること

---

## フェーズ3: レシピ取得

> 完了条件: 障害時フォールバックのテストが緑

### PR 3-A: RecipeLink 型 `feat/recipe-link` ✅

- [ ] 🔧 検索APIを最終選定（Brave / Google CSE）※spec.md 13章 未決事項1
      → **3-C の直前に先送り**。3-B の stub まではキー無しで進められるため。
- [x] 🔴 テスト: URLの検証（http/https のみ、不正なURLを拒否）
- [x] 🔴 テスト: ドメイン抽出（`https://a.example.com/x` → `a.example.com`）
- [x] 🔴 テスト: タイトルが空なら拒否
- [x] 🟢 `domain.RecipeLink` を実装

### PR 3-B: stub gateway `feat/recipe-stub-gateway` ✅

APIキー無しで全機能が動く状態を保つため、実装より先に入れる。

- [x] 🔧 `RecipeSearchGateway` インターフェースを定義（`service/ports.go`）
- [x] 🔴 テスト: 決定的に3件返る（別インスタンス間でも同一）
- [x] 🔴 テスト: 献立名がタイトルに含まれる
- [x] 🔴 テスト: `limit` で件数が絞られる（0・負数・3超の境界）
- [x] 🔴 テスト: 献立名が空なら `ErrEmptyMenuName`
- [x] 🟢 stub gateway を実装（`internal/gateway`）

### PR 3-C: 実 gateway の正常系 `feat/recipe-search-gateway`

> 検索APIは **Brave** に決定（spec.md 13.1）。Google CSE は新規申込が締め切られたため。
> 本PRは `httptest.Server` でHTTPをスタブするため、**APIキー無しで実装・テストできる**。
> 実キーが要るのは 3-F の実機確認から。

- [x] 🔴 テスト: 正常系で3件返る（httptest.Server でスタブ）
- [x] 🔴 テスト: 3件未満しか返らない場合はその件数
- [x] 🔴 テスト: 4件以上返っても3件に切り詰める
- [x] 🔴 テスト: 0件でも成功扱い（空スライス）
- [x] 🔴 テスト: スニペットの `<strong>` を除去し、HTMLエンティティを戻す
- [x] 🔴 テスト: 壊れた1件は飛ばして残りを返す
- [x] 🔴 テスト: APIキーはヘッダのみで渡し、URLに載せない
- [x] 🟢 実 gateway を実装

### PR 3-D: 実 gateway の異常系 `feat/recipe-gateway-resilience`

- [x] 🔴 テスト: HTTP 500 でエラー
- [x] 🔴 テスト: 不正なJSONでエラー
- [x] 🔴 テスト: タイムアウト（3秒）※試行ごと
- [x] 🔴 テスト: 指数バックオフで最大2回リトライ
- [x] 🔴 テスト: リトライ後も失敗ならエラー
- [x] 🔴 テスト: 4xx はリトライしない（無駄な再試行を避ける）
- [x] 🔴 テスト: **429 だけはリトライする**（レート制限は時間をおけば回復する）
- [x] 🔴 テスト: context が切れたらリトライを打ち切る
- [x] 🟢 タイムアウトとリトライを実装（失敗は `ErrSearchFailed` に包む → 3-F で 502）

> ⚠️ 最悪の所要時間は 3s × 3回 + バックオフ 0.6s ≒ **9.6秒**。spec.md 11章の
> 「レシピ取得 p95 2s以内」は通常時（キャッシュ有り: 数ms / 無し: 1回で成功）の話で、
> 全滅時のテール。全体の締め切りを設けるかは 3-F で判断する。

### PR 3-E: gateway ファクトリ `feat/recipe-gateway-factory`

- [x] 🔴 テスト: `SEARCH_API_PROVIDER=stub` で stub が返る
- [x] 🔴 テスト: `brave` で実装が返る
- [x] 🔴 テスト: 未知の値でエラー（`google_cse` を含む）
- [x] 🔴 テスト: `brave` なのにAPIキーが空ならエラー
- [x] 🔴 テスト: **プロバイダが空でもエラー**（stub に既定しない）
- [x] 🔴 テスト: 表記ゆれ（大文字・前後の空白）は吸収する
- [x] 🟢 ファクトリを実装（環境変数は読まず、値として受け取る）

### PR 3-F: `GET /menus/:id/recipes` `feat/api-get-recipes`

- [x] 🔴 テスト: 200 と3件
- [x] 🔴 テスト: 存在しない献立で 404
- [x] 🔴 テスト: gateway 障害で 502（`service.ErrRecipeSearchFailed` → 502）
- [x] 🔴 テスト: 3件未満でも200 / 0件でも200で空配列
- [x] 🔴 テスト: 不正なUUIDで400（service を呼ばない）
- [x] 🟢 `MenuHandler.Recipes` と `service.RecipeLinks` を実装
- [x] 🔧 `main.go` にファクトリ経由で結線（プロバイダ不正なら起動時エラー）
- [x] 🔧 3-D の最悪9.6秒に対し、全体の締め切りを設けるか判断する
      → **設ける**。`service` 側で5秒の上限を課した。gateway は自身の締め切り超過を
        素の context エラーで返すため、それを `ErrRecipeSearchFailed` に寄せて 502 にする。
        呼び出し側の中断（利用者が画面を離れた）とは区別する。
- [x] 🔧 実機確認: stub で3件返ること
- [x] 🔧 実機確認: **Brave の実キーで本物のレシピが3件返ること**
      → 「かつ丼」で デリッシュキッチン / キッコーマン / ニチレイフーズ が0.54秒で返った
- [x] 🔧 実機確認: **日本語のレシピが返ること**。返らなければ `country` / `search_lang` を付ける
      → **必要だった**。無指定だと「親子丼」の3件中2件が英語版（`kurashiru.com/us/`、
        `cookpad.com/eng/`）。`country=JP` / `search_lang=jp` を追加して解消。
        なお **`search_lang=ja` は 422 で拒否される**（実測）。3-C で推測せず保留した判断が正しかった。

### PR 3-G: レシピリンクのキャッシュ `feat/recipe-link-cache`

spec.md 13.2 で MVP に含めると決定。献立120件固定のため、キャッシュすれば
API消費は生涯約120クエリで頭打ちになる。

- [x] 🔧 マイグレーション `000002_create_recipe_link_caches`（menu_id PK / links jsonb / fetched_at）
- [x] 🔴 テスト: 初回は gateway を呼び、2回目は呼ばない
- [x] 🔴 テスト: TTL 7日を過ぎたら再取得する（境界値: 7日ちょうどはヒット / 7日と1秒で失効）
- [x] 🔴 テスト: キャッシュが壊れていても gateway にフォールバックする
- [x] 🔴 テスト: gateway が0件を返した場合もキャッシュする（毎回叩き直さない）
- [x] 🔴 テスト: gateway 障害時はキャッシュを書かない
- [x] 🔴 テスト: 書き込みが失敗しても検索結果は返す
- [x] 🔴 統合テスト: 保存・上書き・CASCADE・壊れた値の拒否
- [x] 🟢 キャッシュを実装（**service に持たせた**。キャッシュのキーが `menu_id` である一方、
      `RecipeSearchGateway.Search` は献立名しか受け取らないため、gateway を包む形にはできない）
- [x] 🔧 実機確認: 実キーで **0.565秒 → 0.0033秒**（約170倍）。DBに1件×3リンクが保存された

---

## フェーズ4: 週間献立

> 完了条件: 重複回避と枯渇時の緩和テストが緑

### PR 4-A: 週間献立の骨格 `feat/suggest-weekly-basic`

> 起点は **当日** に決定（spec.md 13.3）。サーバは曜日を持たず `day` 1..7 のみを返す。

- [x] 🔧 週の開始曜日を決める ※spec.md 13章 未決事項4 → **当日起点**（13.3）
- [x] 🔴 テスト: 7件返る
- [x] 🔴 テスト: `day` が 1..7 の連番（起点当日が 1）
- [x] 🔴 テスト: 候補0件で `ErrNoMenuFound`
- [x] 🔴 テスト: 候補は1度だけ問い合わせる（日ごとに引くとDB負荷が7倍）
- [x] 🔴 テスト: 不正な条件はDBに問い合わせず弾く
- [x] 🔴 テスト: 骨格の段階では重複を許す（4-B での変化を差分で見えるようにする）
- [x] 🟢 `service.SuggestWeekly` の骨格を実装（`domain.DayMenu` / `domain.WeekLength`）

### PR 4-B: 同一献立の重複回避 `feat/weekly-no-duplicate-menu` ✅

- [x] 🔴 テスト: 同一献立が週内に2度出現しない
- [x] 🔴 テスト: 候補がちょうど7件なら7件とも異なる（境界値）
- [x] 🔴 テスト: 選ばれた献立は以降の候補から外れる
- [x] 🔴 テスト: 候補が7件未満なら現時点では `ErrNoMenuFound`（**4-D で緩和する中間状態**）
- [x] 🟢 重複回避を実装（候補から取り除く方式。再抽選ループは使わない）

### PR 4-C: 同ジャンル3連続の回避 `feat/weekly-no-genre-streak` ✅

- [x] 🔴 テスト: 同一ジャンルが3日以上連続しない
- [x] 🔴 テスト: 2連続は許容される
- [x] 🔴 テスト: 連続の判定は直前2日だけを見る（週内の出現回数は数えない）
- [x] 🔴 テスト: 重複回避と連続回避が同時に効く
- [x] 🔴 テスト: 候補が全て同一ジャンルなら現時点では `ErrNoMenuFound`（**4-D で緩和**）
- [x] 🟢 ジャンル連続の回避を実装

> ⚠️ **ジャンルで絞った週間献立が現時点では必ず失敗する**。候補が全て同一ジャンルに
> なり、件数が足りていても3日目で選べる候補が尽きるため。spec.md 2.2 はジャンル指定を
> 許しているので 4-D で必ず解消すること。週間献立のAPIは 4-F まで公開されないため
> 利用者には見えない。

### PR 4-D: 候補枯渇時の緩和 `feat/weekly-relaxation` ✅

ここが週間献立の最も壊れやすい部分。単独のPRで集中的にテストする。

- [x] 🔴 テスト: 候補が6件のとき緩和して同一献立の再利用を許す
- [x] 🔴 テスト: 候補が1件のとき7日とも同じ献立になる（極端値）
- [x] 🔴 テスト: 候補が全て同一ジャンルなら3連続禁止を緩和する ← **4-C の制約を解消**
- [x] 🔴 テスト: 緩和が起きたことを呼び出し側が判別できる（`DayMenu.Relaxed*`）
- [x] 🔴 テスト: 緩和は必要最小限（7件・ちょうど7件とも緩和しない）
- [x] 🔴 テスト: **絞り込み条件は緩めない**（利用者の指定を勝手に外さない）
- [x] 🔴 テスト: 緩和の順序（重複よりジャンル連続を先に緩める）
- [x] 🟢 段階的な緩和ロジックを実装（4段階）

> 緩める順序は spec.md 2.2 のルール列挙順の逆（重要度の低いものから）。
> 候補が7件以上あれば重複は決して起きない（残りが尽きないため）。
>
> **先読みはしない。** 週全体では3連続を避けられる並びがあるのに袋小路に入ることが
> 理論上ある。候補1〜12件 × ジャンル1〜4種を総当たり2400回試して5回のみ、いずれも
> 2ジャンルしか無い作為的な形。実マスタ相当（120件・4ジャンル）は2000回試して0回。

### PR 4-E: 履歴除外の連携 `feat/weekly-exclude-history` ✅

- [x] 🔴 テスト: 直近履歴に含まれる献立を避ける
- [x] 🔴 テスト: 履歴を除くと7件に満たない場合は履歴除外を緩和する
- [x] 🔴 テスト: 履歴除外は重複回避より先に緩む
- [x] 🔴 テスト: **連続を緩めても履歴は避ける**（段階の抜けをストレステストが検出）
- [x] 🔴 テスト: 全候補が履歴にあっても7日分を返す（極端値）
- [x] 🔴 テスト: **履歴を repository の絞り込みに使わない**
- [x] 🟢 履歴除外を実装（緩和は8段階に。フェーズ6で実データと結線）

> 履歴は `SuggestWeekly` の第3引数で渡す。`MenuFilter.ExcludeIDs` に入れてはならない。
> あちらは repository が SQL で除外する**強い条件**で、緩和の余地が無くなるため。
>
> 「履歴の献立を出す」と「ジャンル3連続にする」の二択では**前者を採る**。
> spec.md 2.2 がルール3(履歴)にだけ「候補が枯渇する場合はこの条件を緩和する」と
> 明記しているため。履歴を避けられる候補が残っていても履歴の献立が出ることがある。

### PR 4-F: `POST /menus/suggest-weekly` `feat/api-suggest-weekly` ✅

- [x] 🔴 テスト: 200 と `week` 配列7件
- [x] 🔴 テスト: 不正なリクエストボディで 400（壊れたJSON・配列・型違い・未知の値）
- [x] 🔴 テスト: 候補0件で 422
- [x] 🔴 テスト: 未指定・null・空文字はいずれも絞り込まない
- [x] 🔴 テスト: ボディが空でも 200（条件なしの提案）
- [x] 🔴 テスト: POST 以外は受け付けない
- [x] 🟢 `MenuHandler.SuggestWeekly` を実装
- [x] 🔧 実機確認: 7日分・重複なし・3連続なし（0.008秒）
- [x] 🔧 実機確認: **和食×凝っている（候補10件・全て同一ジャンル）でも7日分**が重複なしで返る
      → 4-C で「ジャンル絞りは必ず失敗する」としていた制約が 4-D で解消済みだと実データで確認

> 緩和の情報（`DayMenu.Relaxed*`）はレスポンスに含めない。spec.md 5.1 のレスポンスに
> 無いため。画面に出す必要が生じたら仕様から決める（8-G）。
>
> `GET /menus/suggest-weekly` は 405 ではなく 400 になる。`GET /menus/:id` が
> `suggest-weekly` を献立IDとして拾い、UUIDでないため弾かれる。素通りはしないので実害なし。

### PR 4-G: 1日だけの引き直し `feat/api-reroll-day` ✅

- [x] 🔧 **spec.md 5.1 に `POST /menus/reroll-day` を追記**（2.2 が要求しているのに API 一覧に無かった）
- [x] 🔴 テスト: 指定日だけ変わり、他の日は保持される
- [x] 🔴 テスト: 引き直し後も重複回避が効く
- [x] 🔴 テスト: 範囲外の day で 400 / 週が7件でなければ 400
- [x] 🔴 テスト: **前後どちらの連続も避ける**（挟まれた形も含む）
- [x] 🔴 テスト: 同じ献立を返さない（候補に余裕があるとき）
- [x] 🔴 テスト: 候補が週と同数なら同じ献立が返る（重複を作るより据え置く）
- [x] 🔴 テスト: 他の日はまとめて引く（`FindByIDs` 1回）
- [x] 🟢 引き直しAPIを実装（`service.RerollDay` / `MenuRepository.FindByIDs`）
- [x] 🔧 実機確認: 和食で絞って3日目を5回引き直し、毎回異なり重複0

> サーバは週の状態を持たない。現在の週をリクエストで受け取り、引き直した1日だけを返す。
>
> 連続判定は `SuggestWeekly` の「直前2日」ではなく**前後両側**を見る。3日目を引き直すなら
> [1,2,3] [2,3,4] [3,4,5] のどの窓でも3連続にしない。
>
> 引き直す日の献立自身も優先候補から外す（同じものが返るのでは引き直しにならない）。
> ただし候補が週と同数なら、重複を作るより据え置く（重複回避が最重要ルールのため）。

---

## フェーズ5: 認証

> 完了条件: 認証境界のテストが緑

### PR 5-A: users / auth_identities `feat/auth-schema` ✅

- [x] 🔧 マイグレーション `000003_create_users`
      → 番号は **`000002` ではなく `000003`**。`000002` は 3-G の
        `recipe_link_caches` で使用済みのため繰り上げた。
- [x] 🔴 テスト: CHECK制約（password なら hash 必須）
- [x] 🔴 テスト: CHECK制約（google なら uid 必須）
- [x] 🔴 テスト: UNIQUE (provider, provider_uid)
- [x] 🔴 テスト: メールの UNIQUE
- [x] 🔴 テスト: user 削除で identity も消える（CASCADE）
- [x] 🟢 マイグレーションを実装（users / auth_identities）
      → 統合テストの後始末 `TRUNCATE` に users を追加（menus と参照が無く、
        メールの UNIQUE がテスト間で衝突するため）。

### PR 5-B: パスワードのハッシュ化 `feat/password-hashing` ✅

- [x] 🔴 テスト: ハッシュ化と検証（bcrypt cost 12）
- [x] 🔴 テスト: 同じパスワードでも毎回異なるハッシュ
- [x] 🔴 テスト: 誤ったパスワードで検証が失敗（`ErrPasswordMismatch`）
- [x] 🔴 テスト: 8文字未満を拒否（**文字数**で数える。全角8文字は通る）
- [x] 🔴 テスト: 72バイト超の扱い（bcryptの上限）→ **切り詰めず拒否**
- [x] 🔴 テスト: 壊れたハッシュは不一致と区別する
- [x] 🟢 パスワードユーティリティを実装（`internal/auth`）

> **72バイト超は切り詰めず拒否**する。切り詰めると別々のパスワードが同じ
> ハッシュになりうるため。最小長は spec.md 11章の「8文字以上」に忠実に
> ルーン数で数える（全角8文字＝24バイトでも通す）。検証の不一致
> （`ErrPasswordMismatch`）と壊れたハッシュのエラーは呼び出し側が
> 区別できるよう分ける。`golang.org/x/crypto` が direct 依存に昇格。

### PR 5-C: サインアップ `feat/signup` ✅

- [x] 🔴 テスト: user と auth_identity が作られる（統合: 対で1件ずつ・トランザクション）
- [x] 🔴 テスト: 登録済みメールで 409（`ErrEmailTaken`。制約名まで見て判定）
- [x] 🔴 テスト: メール形式が不正で 400（`domain.ErrInvalidEmail`）
- [x] 🔴 テスト: パスワードが短いと 400（`auth.ErrPasswordTooShort`）
- [x] 🟢 `service.SignUp` と `POST /auth/signup` を実装
- [x] 🔧 実機確認: 正常→201 / **大文字メールの再登録→409**（正規化が効く）/
      不正メール→400 / 短いパスワード→400。DBに user 1件 + password identity
      1件（hash は `$2a$12$`）

> **表示名はメールのローカル部から導出**する。サインアップは表示名を受け取らない
> （spec.md 5.2）が users.display_name は NOT NULL のため。
>
> ドメインに `Email`（正規化・検証つき）/ `User` / `UserID` を追加。メールは
> 小文字に正規化して持ち、大小違いでの二重登録を防ぐ。パスワードのハッシュ化は
> `PasswordHasher` として注入（乱数源と同じDIの形。テストは実物 `auth.Hasher`）。
> user と auth_identity は必ず対で作るため repository でトランザクションに束ねる。
> **成功時は 201。Cookie/JWT の発行は 5-E / 5-F で行う**。

### PR 5-D: ログイン `feat/login` ✅

- [x] 🔴 テスト: 正しいパスワードで成功
- [x] 🔴 テスト: 誤ったパスワードで 401
- [x] 🔴 テスト: 存在しないメールで 401（存在時と**同一の**エラー＝ユーザー列挙対策）
- [x] 🔴 テスト: Google のみのユーザーがパスワードログインを試みると 401
- [x] 🟢 `service.Login` と `POST /auth/login` を実装
- [x] 🔧 実機確認: 正しいパスワード→200 / 誤り→401 / 存在しないメール→**2と同一の401** /
      大文字メールも正規化して200。応答時間も ~0.19s で一致（タイミング等化が効く）

> **3つの失敗（メール不在・パスワード違い・Google のみ）を全て `ErrInvalidCredentials`
> (401) に丸める**。エラーの差からアカウントの有無を推測されないようにするため。
> repository は `provider='password'` を内部結合で引くので、Google のみのユーザーは
> 自然に0行＝`ErrCredentialNotFound` になる。
>
> **タイミング等化**: 照合対象が無いときも固定ハッシュで bcrypt を1回走らせ、
> 「存在するがパスワード違い」と応答時間を揃える。即座に返すと時間差で存在が漏れるため。
>
> 成功時は 200。Cookie/JWT の発行は 5-E / 5-F。メール形式の不正だけは 400
> （照合以前の問題で存在推測に使えないため）。

### PR 5-E: JWT の発行と検証 `feat/jwt` ✅

- [x] 🔴 テスト: アクセストークンの発行と検証
- [x] 🔴 テスト: 有効期限切れを拒否（15分。exp は排他的で15分ちょうどは失効）
- [x] 🔴 テスト: 署名が異なるトークンを拒否
- [x] 🔴 テスト: `alg=none` を拒否
- [x] 🔴 テスト: 改竄されたペイロードを拒否
- [x] 🔴 テスト: 空の秘密鍵での生成を拒否
- [x] 🟢 JWT の発行・検証を実装（`internal/auth`、golang-jwt/v5）

> **署名方式を HS256 に固定**（`WithValidMethods`）。これで `alg=none`（署名なし）や、
> 公開鍵を秘密鍵と取り違えさせる非対称方式へのすり替えをまとめて拒否する。
> 失敗の内訳（期限切れ/署名不正/改竄）は攻撃者への手掛かりになるため、外向きには
> `ErrTokenInvalid` 1つに丸める。`exp` は RFC 7519 通り排他的（現在時刻が exp より
> 前でなければ失効）なので、15分ちょうどは失効が正しい。`now` を差し替え可能にし、
> 期限判定をテストと本番で同じ経路に通す。ライブラリ単体のため main 結線は 5-F 以降。

### PR 5-F: Cookie とリフレッシュ `feat/auth-cookie` ✅

- [x] 🔴 テスト: Cookie 属性（HttpOnly / Secure / SameSite=Lax）
- [x] 🔴 テスト: リフレッシュトークンで再発行（30日。exp は排他的）
- [x] 🔴 テスト: 期限切れリフレッシュトークンで 401
- [x] 🔴 テスト: ログアウトで Cookie が失効する
- [x] 🔴 テスト: **リフレッシュ⇄アクセスの取り違えを拒否**（種別 typ クレーム）
- [x] 🔴 テスト: リフレッシュ Cookie 欠落で 401
- [x] 🟢 Cookie の発行・失効と `/auth/refresh`、`/auth/logout` を実装
- [x] 🔧 実機確認: ログインで両Cookie発行（access Max-Age=900/refresh 2592000）/
      refresh(Cookie有)→204・(無)→401 / logout で両Cookie Max-Age=0

> **リフレッシュは stateless**（署名付きJWT・30日）。スキーマにトークン表が無く、
> spec.md 66-67 も「JWTで受け渡し」のため。ログアウトは Cookie 失効で表す。
>
> **種別クレーム `typ`(access/refresh) を追加**。これが無いと長寿命の
> リフレッシュトークンを短寿命のはずのアクセストークンとして使い回せてしまう。
> **リフレッシュ Cookie はパスを `/api/v1/auth` に絞る**（長寿命トークンの露出範囲を最小化）。
> サインアップ／ログイン成功時に両Cookieを発行（サインアップは自動ログイン）。
> `/auth/refresh` はアクセスCookieだけ差し替えて 204、`/auth/logout` は冪等で 204。
> `main.go` は `JWT_SECRET` 未設定なら起動時エラー。

### PR 5-G: 認証ミドルウェア `feat/auth-middleware` ✅

- [x] 🔴 テスト: 認証必須エンドポイントに未認証で 401（Cookie欠落・不正トークン）
- [x] 🔴 テスト: 有効なCookieでコンテキストにユーザーが入る（service に userID が渡る）
- [x] 🔴 テスト: 献立検索は未認証でも 200（menu ルートを包まないことで担保）
- [x] 🔴 テスト: `GET /auth/me` が現在のユーザーを返す
- [x] 🔴 テスト: リフレッシュトークンをアクセス Cookie に載せても 401（種別違い）
- [x] 🟢 ミドルウェア `RequireAuth` と `/auth/me` を実装
- [x] 🔧 実機確認: 未認証 /auth/me→401 / ログイン→Cookie→200 /
      献立検索は未認証でも200 / ログアウト後→401

> `RequireAuth` はアクセス Cookie を検証し、userID を echo コンテキストに載せる。
> 認証不要のエンドポイント（献立検索）は**包まないことで未認証でも 200 のまま**。
> `/auth/me` はトークンの sub から DB を引いてユーザーを返す（`UserRepository.FindByID`
> / `AuthService.CurrentUser`）。有効なトークンが指すユーザーが消えていたら 401
> （`ErrUserNotFound` → 401）。フェーズ5の認証境界はここで完成（残りは 5-H/5-I の Google SSO）。

### PR 5-H: Google SSO の認可URL `feat/google-auth-url` ✅（コード）

- [x] 🔧 Google Cloud Console で OAuth クライアントを作成（2026-07-22 完了）
      → 本番クライアントを作成し、承認済みリダイレクトURIに
        `https://kondatekun.yuuyakim.com/api/v1/auth/google/callback` を登録。
        本番でGoogleログインが通ることを確認済み。
        開発用の `http://localhost:8080/api/v1/auth/google/callback` も併記のまま残す。
- [x] 🔴 テスト: 認可URLに PKCE の code_challenge が含まれる（S256）
- [x] 🔴 テスト: state が生成され Cookie に保存される（URL の state と一致）
- [x] 🔴 テスト: state は毎回異なる
- [x] 🔴 テスト: Cookie 属性（HttpOnly / Secure / SameSite=Lax / Path=/api/v1/auth）
- [x] 🔴 テスト: 未設定（client_id 空）なら 503
- [x] 🟢 `GET /auth/google` を実装（`internal/auth` の `GoogleOAuth`、x/oauth2）
- [x] 🔧 実機確認: 未設定→503 / ダミー設定→302 で Google 認可URLへ
      （code_challenge・state・scope が乗り、state と oauth_state Cookie が一致）

> Google 認可フローは **Authorization Code + PKCE**。verifier と CSRF 対策の
> state を生成し、短命 Cookie（10分・SameSite=Lax）に保存してから認可URLへ 302。
> **SameSite=Lax はコールバック（トップレベルGET遷移）で Cookie を送るために必須**
> （Strict だと送られない）。認可URLの生成は x/oauth2 に任せる（5-I のトークン交換でも使う）。
> Google 未設定でも起動し、`/auth/google` だけ 503 にする（任意機能）。

### PR 5-I: Google SSO のコールバック `feat/google-callback` ✅（コード）

- [x] 🔴 テスト: state 不一致で 401（CSRF対策）
- [x] 🔴 テスト: state が無い場合も 401
- [x] 🔴 テスト: code_verifier 不一致で 401（Exchange 失敗を 401 に丸める）
- [x] 🔴 テスト: 初回コールバックで user が作られる（統合）
- [x] 🔴 テスト: 2回目は既存 user に紐づく（重複作成しない）（統合）
- [x] 🔴 テスト: **既存のパスワードユーザーと同じメール**なら同一userに identity を追加（統合）
- [x] 🔴 テスト: メール未確認は拒否（紐付け乗っ取り対策）
- [x] 🟢 `GET /auth/google/callback` を実装
- [x] 🔧 実機確認(自動): callback 結線（state無し→401）/ パスワード回帰（サインアップ→/auth/me→200）
- [x] 🔧 実機確認(ブラウザ): **Google 実ログイン通し** → 成功。/auth/me が
      `yuuya.kim0801@gmail.com`「キムさん」を返し、DBに user + google identity
      （provider_uid あり・password_hash なし）が作られた

> コールバックは **state を Cookie と定数時間比較（CSRF対策）** → コードを本人情報に交換
> （x/oauth2、verifier で PKCE 突合）→ ユーザー upsert → 認証 Cookie 発行 → フロントへ 302。
> **メール未確認は拒否**（未確認メールでの紐付けは乗っ取りに使えるため）。
> upsert は3分岐（既存google / 同一メールに紐付け / 新規）をトランザクションで。
> トークン交換・userinfo は httptest でスタブ、ハンドラは fake で検証。
> 実フローは実クレデンシャル＋ブラウザ同意が要るためユーザーが踏む。

---

## フェーズ6: 履歴

> 完了条件: 16件目でFIFOが働くテストが緑

### PR 6-A: search_histories `feat/history-schema` ✅

- [x] 🔧 マイグレーション `000004_create_search_histories`
      → 番号は `000003` ではなく **`000004`**（`000003` は users で使用済み）。
- [x] 🔧 INDEX (user_id, searched_at DESC)
- [x] 🔴 テスト: 履歴を1件記録できる
- [x] 🔴 テスト: user 削除で履歴も消える（CASCADE）
- [x] 🔴 テスト: `SearchMode`(single/weekly) の検証（ドメイン）
- [x] 🟢 マイグレーションと Repository の記録を実装
- [x] 🔧 実機確認: migrate version 4 適用、INDEX・CHECK・FK を確認

> `search_mode` は single/weekly を CHECK と `domain.SearchMode` の両方で縛る。
> menu_id の FK は CASCADE を付けない（献立マスタは固定で削除しない）。
> FIFO（15件超の削除）は 6-B で。ここは1件INSERTのみ。

### PR 6-B: FIFO 15件 `feat/history-fifo` ✅

フェーズ6の核。単独のPRで集中的にテストする。

- [x] 🔴 テスト: 15件までは削除されない（境界値）
- [x] 🔴 テスト: **16件目の投入で最古が消え、ちょうど15件残る**
- [x] 🔴 テスト: FIFOはユーザー単位（他ユーザーの履歴が消えない）
- [x] 🔴 テスト: 21件を一度に入れても15件に収まる
- [x] 🔴 テスト: `searched_at` が同値でも順序が安定する（タイブレーク＝seq）
- [x] 🟢 `service.RecordHistory` を実装（アプリ層・トランザクション内）
- [x] 🔧 実機確認: migrate 000005 up/down/up、seq 列と3列インデックスを確認

> **タイブレーク用に `seq`(bigint IDENTITY) 列を追加**（migration 000005）。
> `searched_at` は now()=トランザクション時刻で、週間一括登録では7件が同値になる。
> `ORDER BY searched_at DESC` だけでは同値の行の順序が定まらず「最新15件」が
> 非決定的になるため、`searched_at DESC, seq DESC` で挿入順に確定する。
> FIFO は repository が INSERT+DELETE を1トランザクションで（spec.md 4.3）。
> 保持件数15は業務ルールとして service（`HistoryLimit`）が repository に渡す。

### PR 6-C: 週間献立の一括記録 `feat/history-bulk-record` ✅

- [x] 🔴 テスト: 7件を1トランザクションで記録
- [x] 🔴 テスト: FIFOが7回ではなく1回だけ走る（全件INSERT後に1度だけ prune）
- [x] 🔴 テスト: 途中で失敗したら全件ロールバック（FK違反で0件）
- [x] 🟢 一括記録を実装（`RecordManyWithLimit` / `HistoryService.RecordMany`）

> 7件を1トランザクションで INSERT し、**prune は最後に1度だけ**走らせる
> （挿入ごとに走らせる必要はなく無駄）。seq タイブレークにより一括の7件は
> 同一 searched_at でも挿入順が保たれる。途中失敗は全件ロールバック。

### PR 6-D: `GET /histories` `feat/api-list-histories` ✅

- [x] 🔴 テスト: 新しい順に返る（searched_at DESC, seq DESC）
- [x] 🔴 テスト: 未認証で 401
- [x] 🔴 テスト: 他ユーザーの履歴は返らない（user_id で絞る）
- [x] 🔴 テスト: 0件のとき空配列（null ではなく []）
- [x] 🟢 `GET /histories` を実装
- [x] 🔧 実機確認: 未認証→401 / ログイン→空配列 / 履歴1件で献立情報つき一覧

> 読み取りモデル `domain.HistoryEntry`（HistoryID + Menu + Mode + SearchedAt）を追加。
> repository が menus を JOIN して組み立てる。並びは 6-B の seq タイブレークに揃える。
> 認証済み userID は RequireAuth がコンテキストに載せたものを使い、SQL で user_id を
> 絞るので他人の履歴は構造上返らない。個別削除(6-E)に使うため履歴IDも返す。

### PR 6-E: 履歴の削除 `feat/api-delete-histories` ✅

- [x] 🔴 テスト: 個別削除（204）
- [x] 🔴 テスト: 他ユーザーの履歴削除で 403（`ErrHistoryForbidden`）
- [x] 🔴 テスト: 存在しない履歴の削除で 404（`ErrHistoryNotFound`）
- [x] 🔴 テスト: 全件削除（他ユーザーの履歴は残る）
- [x] 🔴 テスト: 不正なUUIDで 400 / 未認証で 401
- [x] 🟢 `DELETE /histories/:id` と `DELETE /histories` を実装
- [x] 🔧 実機確認: 個別204 / 存在しない404 / 不正UUID400 / 全件204 / 削除後は空配列

> **所有権を先に確認してから消す**。DELETE の件数だけでは「存在しない(404)」と
> 「他人のもの(403)」を区別できないため、先に user_id を引いて判定する。
> spec は他人の履歴を 403 と定めるため、存在の有無が漏れることは許容する。
> 静的な `DELETE /histories`（全件）は `:id` より優先照合され飲み込まれない。

### PR 6-F: 検索フローへの結線 `feat/wire-history` ✅

- [x] 🔴 テスト: 献立提案時に履歴が記録される（single / weekly）
- [x] 🔴 テスト: **未認証時は履歴を記録しない（エラーにしない）**
- [x] 🔴 テスト: 検索時に直近履歴が除外候補として渡る
- [x] 🔴 テスト: 履歴記録の失敗が献立提案を失敗させない
- [x] 🔴 テスト: 直近履歴の取得失敗でも提案は返す / 履歴除外で枯渇したら緩和
- [x] 🟢 結線を実装（OptionalAuth + MenuHistory）
- [x] 🔧 実機確認: 認証3回で3回とも異なる献立（除外が効く）/ 未認証は記録なし /
      /histories はちょうど3件

> **OptionalAuth** ミドルウェアを新設（Cookie が無くても拒否せず素通り、あれば
> userID を載せる）。献立検索を未認証でも 200 に保つため。**ルート単位で付ける**
> （グループ全体に付けると echo の 405 判定が 404 に変わるため）。
> 単発検索は履歴を `ExcludeIDs`（ハード除外）に足し、**除外で枯渇したら外して再試行**。
> 週間/引き直しは第3引数 `recentIDs`（4-E のソフト緩和）で渡す。引き直しは
> 確定前の操作なので記録しない。記録・取得の失敗は best-effort（ログのみ、提案は成功）。

---

## フェーズ7: お気に入り

> 完了条件: 重複追加が409になるテストが緑

### PR 7-A: favorites `feat/favorites-schema` ✅

- [x] 🔧 マイグレーション `000006_create_favorites`（UNIQUE (user_id, menu_id)）
      → 番号は `000004` ではなく **`000006`**（004=search_histories, 005=seq で使用済み）。
- [x] 🔴 テスト: UNIQUE 制約が効く（同一ユーザー×同一献立の二重登録を拒否）
- [x] 🔴 テスト: 別ユーザーなら同じ献立を登録できる
- [x] 🔴 テスト: user 削除で消える（CASCADE）
- [x] 🟢 マイグレーションを実装
- [x] 🔧 実機確認: migrate 000006 up/down/up、UNIQUE・INDEX・FK を確認

> menu_id の FK は CASCADE を付けない（献立マスタは固定）。user_id は CASCADE。
> INDEX (user_id, created_at DESC) は一覧表示の主経路。重複追加の 409 は 7-B。

### PR 7-B: お気に入りの追加 `feat/api-add-favorite` ✅

- [x] 🔴 テスト: 追加できる
- [x] 🔴 テスト: **同一献立の重複追加で 409**
- [x] 🔴 テスト: 存在しない献立で 404
- [x] 🔴 テスト: 未認証で 401
- [x] 🔴 テスト: 不正な献立IDで 400 / 壊れたボディで 400
- [x] 🟢 `POST /favorites` を実装
- [x] 🔧 実機確認（201 / 409 / 404 / 401 / 400）

> 重複と存在しない献立はどちらも DB の制約に判定させ、SQLSTATE と制約名で
> 振り分ける（23505 + favorites_user_menu_uniq → 409、23503 +
> favorites_menu_id_fkey → 404）。事前に SELECT で確かめる方式は確認と INSERT の
> 間に他リクエストが割り込むとすり抜けるため採らない。制約名まで見るのは
> user_id 側の外部キー違反と取り違えないため。
> レスポンスはお気に入り自体のIDを出さず menuId だけ返す。削除も
> `DELETE /favorites/:menuId` で献立IDを使う（7-C）ため、利用側に不要。

### PR 7-C: お気に入りの一覧と削除 `feat/api-list-delete-favorite` ✅

- [x] 🔴 テスト: 一覧が新しい順に返る
- [x] 🔴 テスト: 他ユーザーのものは返らない
- [x] 🔴 テスト: 削除できる
- [x] 🔴 テスト: 他ユーザーのもの削除は **403 ではなく 404**（下記）
- [x] 🔴 テスト: **15件を超えても自動削除されない**（履歴との違い）
- [x] 🟢 `GET /favorites` と `DELETE /favorites/:menuId` を実装
- [x] 🔧 実機確認（一覧の順序・ユーザー分離・204 / 404 / 400 / 401）

> **403 ではなく 404 にした。** 当初の想定は履歴に合わせた 403 だったが、
> お気に入りの削除はパスに献立ID（`:menuId`）を取る。削除は
> `WHERE user_id = $1 AND menu_id = $2` で絞るため、他人の行には構造上
> たどり着けず、403 を返す状態が発生しない。仮に返すと「その献立は
> 他人が登録している」と教えることになり、他人の登録内容が漏れる。
> 履歴が 403 なのは、グローバルな履歴IDで指定するため他人の行を
> 名指しでき、所有者確認が必要だから。指定子が違えば正しい応答も違う。
>
> 所有者を事前 SELECT せず `RowsAffected() == 0` で 404 を判定する
> （履歴と違い所有者確認が要らないので1クエリで済む）。
> 一覧は履歴と違い切り詰めをしない。並びは `created_at DESC`、
> 同値時は `menu_id DESC` をタイブレークにして順序を確定させる。

---

## フェーズ8: フロントエンド

> 完了条件: Vitest + Playwright が緑

### PR 8-A: テスト基盤 `feat/frontend-test-setup` ✅

- [x] 🔧 Vitest + Testing Library + MSW を導入
- [x] 🔧 CI に frontend のテストを追加
- [x] 🔴 サンプルテストが動くことの確認
- [x] 🟢 設定を実装

> MSW は `onUnhandledRequest: 'error'` で運用する。モックし忘れたAPI呼び出しが
> 素通りして「なぜか通った」状態になるのを防ぐため、未定義のリクエストは失敗させる。
> jsdom の URL を `http://localhost:5173` に固定するのは、`/api/...` の相対URLを
> 解決するため（既定のままだと fetch が Invalid URL になる）。
> `afterEach` で `resetHandlers` と `cleanup` を必ず走らせ、テスト間の依存を断つ。
> これが効いていることをテスト自体でも確認している（上書き → 次のテストで元に戻る）。

### PR 8-B: UI基盤 `feat/frontend-foundation` ✅

- [x] 🔧 Tailwind CSS を導入
- [x] 🔧 React Router を導入
- [x] 🔧 TanStack Query を導入
- [x] 🔴 ルーティングと Query の疎通テスト
- [x] 🔧 実機確認（dev server で / と /histories が 200）

> **OpenAPI の型生成は 8-B2 に分けた。** `api/openapi.yaml` がまだ無く、
> 全エンドポイントの仕様を書き起こす作業は分量も判断も別物のため。
>
> ルータは App の中に置かず、包む側（本番=BrowserRouter / テスト=MemoryRouter）を
> 差し替えられるようにした。QueryClient はモジュールスコープに1つ持つ
> （再描画のたびに作り直すとキャッシュが消えるため）。テストは
> `createTestQueryClient` で毎回新しいインスタンスを使い、キャッシュを持ち越さない。
>
> 🐛 依存を追加すると dev コンテナが起動しなくなる問題に当たった。
> node_modules が匿名ボリュームのため、イメージを再ビルドしても古いままになる。
> `make deps`（`docker compose up -d -V`）を追加した。

### PR 8-B2: OpenAPI から型を生成 `feat/api-types` ✅

- [x] 🔧 `api/openapi.yaml` を起こす（全19エンドポイント）
- [x] 🔧 `openapi-typescript` で `src/api/schema.d.ts` を生成
- [x] 🔧 生成物が最新かをCIで確認する（再生成して `git diff --exit-code`）
- [x] 🔧 実機確認（意図的にyamlを変えて検知することを確認）

> **yaml を手で書いて正とする方式にした。** spec.md 6.3 は「Goハンドラから生成」
> と書いているが、7章のディレクトリ構成は `api/openapi.yaml` を単一の情報源と
> している。前者は swaggo 等のアノテーションを全ハンドラに入れる大工事になるため、
> 後者を採った。
>
> **ただし yaml と Go 実装のズレはこの仕組みでは防げない。** CIが守るのは
> 「yaml と生成された型」の一致だけ。実レスポンスとの一致はフェーズ9の
> 契約テストで担保する（下記に追加）。
>
> TypeScript 6 と openapi-typescript の peer 指定(`^5.x`)が衝突するため、
> package.json の overrides で openapi-typescript の typescript をルートに寄せた。
> `--legacy-peer-deps` は全依存に効いてしまうので使わない。
>
> enum は型としてしか生成されないため、選択肢の値は `src/api/types.ts` に
> `Record<Genre, string>` で持つ。ジャンルが増減すれば型エラーになる。

### PR 8-C: APIクライアント `feat/api-client` ✅

- [x] 🔴 テスト: Cookie が送信される
- [x] 🔴 テスト: problem+json のエラーを解釈する
- [x] 🔴 テスト: ネットワークエラーの扱い
- [x] 🔴 テスト: 204 / problem+json でない失敗 / 壊れた本文
- [x] 🟢 `api/client.ts` を実装

> **失敗を2つの型に分ける。** `ApiError`（応答は返ったが失敗ステータス）は
> status を持ち、401でログインへ・409で重複の案内、と分岐できる。
> `NetworkError`（応答自体を利用できない）は status を持たない。
> 1つの型に混ぜると「401だからログインへ」の分岐が、通信断のときに
> 誤動作する。壊れた本文も後者に含める（ステータスで分岐できない点で同じ）。
>
> problem+json でない失敗（プロキシが返すHTMLなど）でも message は必ず埋める。
> 画面に出す文言が空になる経路を作らないため。
>
> 🐛 CIの「型チェック」が実質無効だったことが発覚。`tsconfig.json` は
> `files: []` のプロジェクト参照構成なので、`tsc --noEmit` は何も検査しない。
> ビルド(`tsc -b`)だけが型エラーを検出していた。CI と Makefile を `tsc -b` に修正。

### PR 8-D: 検索フォーム `feat/ui-search-form` ✅

- [x] 🔴 テスト: ジャンル4種が表示される
- [x] 🔴 テスト: 難易度3種が表示される
- [x] 🔴 テスト: 未選択（すべて）を選べる／選び直して戻せる
- [x] 🔴 テスト: 選んだ条件を送る／片方だけでも送れる／検索中は押せない
- [x] 🟢 検索フォームを実装

> 「すべて」は空文字ではなく **undefined** で親に渡す。バックエンドは
> `genre=` を「絞り込まない」として受けるので空文字でも動くが、
> クエリに出さない方が意図が明確。
>
> 各組を fieldset/legend にした。「すべて」が2つあるため、これが無いと
> 支援技術もテストもどちらの「すべて」か区別できない。
> 見た目は `has-checked:` で label 側に付け、input は `sr-only` で残す
> （キーボード操作とスクリーンリーダーのため）。
>
> 選択肢は `genreLabels` / `difficultyLabels`（8-B2）から生成する。
> ジャンルが増えれば型エラーでラベル追加漏れに気づける。

### PR 8-E: 検索結果 `feat/ui-search-result` ✅

- [x] 🔴 テスト: 検索ボタンで結果が表示される
- [x] 🔴 テスト: ローディング表示
- [x] 🔴 テスト: 422 でメッセージ表示
- [x] 🔴 テスト: 「別の献立を見る」で引き直せる
- [x] 🔴 テスト: 条件がクエリに載る／通信エラー／引き直しの失敗
- [x] 🟢 検索結果表示を実装
- [x] 🔧 実機確認（5173 経由で backend に到達することを確認）

> **useQuery ではなく useMutation を使う。** 検索はボタンを押したときだけ走り、
> 同じ条件でも押すたびに新しい結果が欲しい。useQuery はキーが同じなら再取得を
> 避けるので「別の献立を見る」と噛み合わない。
>
> 検索した条件を state に覚え、引き直しは同じ条件で再実行する
> （フォームを操作し直さなくてよい）。
>
> 失敗時は古い結果を消す。残すとそれが今の結果に見えるため。
>
> ⚠️ 422（条件に合う献立が無い）は**現在のシードデータでは実機で再現できない**。
> ジャンル×難易度の全12通りに献立が存在するため。テストでのみ確認している。

### PR 8-F: レシピ表示 `feat/ui-recipes` ✅

- [x] 🔴 テスト: 3件表示される
- [x] 🔴 テスト: **`target="_blank"` と `rel="noopener noreferrer"`**
- [x] 🔴 テスト: 3件未満でも表示できる／0件でも壊れない
- [x] 🔴 テスト: **502でも献立表示は消えず、レシピ欄だけエラーとリトライ導線**
- [x] 🔴 テスト: 再試行で取り直せる／献立が変われば取り直す／タイトル空でも開ける
- [x] 🟢 レシピ表示を実装
- [x] 🔧 実機確認（5173 経由でレシピ3件）

> **レシピは献立とは別のクエリにする。** 外部の検索APIに依存していて単独で
> 失敗しうるため、失敗をこのコンポーネントの中に閉じ込める。これにより
> 「502でも献立表示は消えない」が構造として保証される（後付けの条件分岐ではない）。
>
> 検索は useMutation だがレシピは **useQuery**。献立IDごとに結果が決まり
> キャッシュが効くうえ、献立が変われば自動で取り直されるため。
>
> 0件は障害ではない（spec.md 2.4）のでエラー扱いにしない。
>
> ⚠️ 502 は 422 と同様、実機では再現できない（スタブの検索プロバイダは
> 失敗しない）。テストでのみ担保している。
>
> 8-E のテスト「検索後に role=status が消える」がレシピ欄の読み込み表示に
> 引っかかった。テスト側が広すぎたので検索中の表示だけを見るよう修正した。

### PR 8-G: 週間献立画面 `feat/ui-weekly` ✅

- [x] 🔴 テスト: 7日分が表示される
- [x] 🔴 テスト: 各日から引き直せる（他の日は変わらない）
- [x] 🔴 テスト: 各日からレシピへ遷移できる
- [x] 🔴 テスト: 当日起点の日付・曜日表示／引き直し失敗でも週は残る／条件を本文で送る
- [x] 🟢 週間献立画面と献立詳細画面を実装
- [x] 🔧 実機確認（7日重複なし → 3日目を引き直し → 詳細取得）

> **献立詳細画面 `/menus/:id` も一緒に作った。** 「各日からレシピへ遷移できる」に
> 遷移先が要るため。履歴・お気に入り（8-J/8-K）からの遷移先にもなる。
>
> 曜日はフロントで決める（spec.md 13.3）。サーバは day を 1..7 で返すだけで
> 曜日を知らない。起点（今日）は prop で受け取り、テストで固定できるようにした。
>
> 引き直しは `onSuccess` でその日だけ差し替える。週全体を取り直さないので
> 他の日は動かない。失敗時も週は残す（失敗した日以外はそのまま使えるため）。
>
> `SearchForm` に `submitLabel` を足して単発検索と共用した（8-D で
> フォームを検索実行から独立させておいた狙いどおり）。
>
> 🐛 レビュー中の動作確認で「レシピを見て戻ると週が消える」不具合が発覚（#55）。
> 週をコンポーネント state に置いていたためルート遷移で消えていた。
> `useSessionState` で sessionStorage に保つようにした。
> 詳細画面の戻り先も `/` 固定だったため、履歴を戻る形に変更。
> あわせてテスト間で sessionStorage が漏れていたのを共通セットアップで解消。

### PR 8-H: 認証画面 `feat/ui-auth` ✅

- [x] 🔴 テスト: サインアップの検証（メール形式、パスワード8文字・境界値含む）
- [x] 🔴 テスト: ログインフォーム
- [x] 🔴 テスト: 401 でエラー表示
- [x] 🔴 テスト: Googleログインボタン
- [x] 🔴 テスト: 409 表示／モード切替／入力し直しで検証メッセージが消える
- [x] 🟢 認証画面を実装
- [x] 🔧 実機確認（201 / 409 / 200 / 401 / me / Google 302）

> **Googleログインは `<a href>`。** fetch で叩くとブラウザがリダイレクトを辿れず、
> 同意画面に行けないうえ Cookie も設定されない。OAuth はブラウザ自身に
> 遷移させる必要がある。テストで href を固定している。
>
> クライアント検証は往復を1回減らすためのもので、**信頼の境界はサーバ**。
> メールの厳密な判定はサーバ（`mail.ParseAddress`）に任せ、ここは
> 明らかな誤りだけを弾く。パスワードは文字単位で数える
> （サロゲートペアを2文字と数えないため）。
>
> ログインと新規登録は入力項目が同じなので1画面で切り替える。
> 成功時は取得したユーザーを `['me']` キャッシュに入れ、遷移先で
> 問い合わせ直さない。

### PR 8-I: 認証ルーティング `feat/ui-auth-routing` ✅

- [x] 🔴 テスト: 未認証でも検索画面は使える（週間献立も）
- [x] 🔴 テスト: 未認証で履歴画面はログインへ誘導（お気に入りも）
- [x] 🔴 テスト: ログイン後は行こうとしていた画面に着く
- [x] 🔴 テスト: **ヘッダの表示名とログアウト**（計画に無かった。下記）
- [x] 🟢 ルーティングとヘッダの認証表示を実装
- [x] 🔧 実機確認（login → me → logout → me 401 → histories 401）

> **ログアウトのUIがどのPRにも計画されていなかった。** バックエンドの
> `/auth/logout` は 5-C で実装済み、クライアント関数も 8-H で用意していたが、
> 押す場所が task.md のどこにも無かった。動作確認の指摘で発覚したため
> ここに含めた。
>
> 判定が付くまでは「未ログイン」と決めつけない。決めつけると認証済みの
> 利用者にも一瞬ログイン画面やログインリンクが見える。
>
> 未認証は 401 で表されるが、これは異常ではなく通常の状態なので
> エラーとして扱わず「ユーザーが居ない」に読み替える。retry も切る
> （401 は再試行しても変わらない）。
>
> 🐛 `setQueryData(key, undefined)` ではキャッシュが消えない。undefined は
> 「更新しない」の意味に解釈されるため、ログアウトしても表示名が残っていた。
> `resetQueries` に変更。
>
> 🐛 動作確認で「ログアウトしたか分かりにくい」との指摘。検索画面はログイン
> 有無で見た目が変わらず、ヘッダの小さな表示が切り替わるだけだったため。
> 「ログアウトしました」を5秒間出すようにした。行き先を変えるより、
> 起きたことを言葉で伝える方が確実に伝わる。
> なお、この指摘から派生した「ホーム画面を設ける」案は 8-M に積んだ。

### PR 8-J: 履歴画面 `feat/ui-history` ✅

- [x] 🔴 テスト: 新しい順に表示（サーバの順をそのまま出す）
- [x] 🔴 テスト: 0件のときの表示
- [x] 🔴 テスト: 1件削除できる／全件削除は確認してから／確認はやめられる
- [x] 🔴 テスト: 検索の種類が分かる／詳細へ遷移／取得・削除の失敗表示
- [x] 🟢 履歴画面を実装
- [x] 🔧 実機確認（3件記録 → 新しい順 → 1件削除 → 全件削除）

> **並べ替えは画面で行わない。** 順序はサーバの責務
> （`searched_at DESC, seq DESC`。同時刻のタイブレークに seq を入れたのは 6-A）。
> 画面でも並べ替えると順序の決定が二重になり、片方だけ直したときに食い違う。
>
> 削除後は手元の配列から抜かず**再取得**する。サーバの状態を正とすることで
> 「消えたつもりで消えていない」食い違いを防ぐ。
>
> 全件削除は取り返しがつかないので確認を挟む。`window.confirm` ではなく
> 画面内の確認にしたのは、テストでグローバルを差し替えずに済むため。
>
> task.md には無かったが**全件削除も実装した**（`DELETE /histories` は 6-D で
> 実装済みで、押す場所が無いままだった。8-I のログアウトと同じ抜け）。
>
> 🐛 動作確認で「検索後に履歴へ遷移しても反映されない（リロードすると出る）」
> との指摘。検索はサーバ側で履歴に記録されるが、フロントが履歴のキャッシュを
> 無効化していなかった。検索・週間献立の成功時に無効化する。
> 引き直しは記録されない（4-F）ので無効化もしない。
>
> 🐛 上の不具合が**テストに映らなかった根本原因**: テスト用 QueryClient が
> `staleTime: 0`（毎回取り直す）で、本番の 30 秒と違っていた。
> `queryDefaults` を1か所に置き、本番とテストで同じ土台を使うようにした。
> テスト環境だけ都合よく設定すると、この種の不具合は永久に検出できない。

### PR 8-K: お気に入り画面 `feat/ui-favorites` ✅

- [x] 🔴 テスト: 追加・削除がトグルする（未ログインでは出さない）
- [x] 🔴 テスト: 一覧表示（新しい順・0件・**15件超でも全件**）
- [x] 🔴 テスト: **追加した献立が遷移するだけで一覧に出る**（キャッシュ無効化）
- [x] 🔴 テスト: 一覧から外せる／失敗時は一覧を残す／詳細へ遷移
- [x] 🟢 お気に入り画面とお気に入りボタンを実装
- [x] 🔧 実機確認（201 → 409 → 一覧 → 204 → 404）

> **お気に入りボタンは MenuCard に載せた。** 献立が出る場所（検索結果・
> 週間献立・詳細）すべてで使えるようにするため。未ログインでは何も
> 描画せず、問い合わせもしない（`enabled: Boolean(user)`）。
>
> 登録済みかの判定は一覧を共有して行う。献立ごとに問い合わせると
> 週間献立の画面で7回走る。
>
> **8-J の教訓を先に適用した。** サーバの状態を変える操作には必ず
> キャッシュの無効化を対で入れ、「追加 → 一覧へ遷移」をテストで固定した。
>
> 🎨 動作確認で「『お気に入りに追加』『お気に入り済み』は分かりにくい」との
> 指摘。似た文言を読み比べないと状態が分からないため、**塗りの有無で表す星**に
> 変更した。図形だけのボタンになるので `aria-label` と `title` で名前を与え、
> `aria-pressed` で状態を伝える。テストは名前で引くので意味は保たれる。
> 一覧側の「外す」は文字のまま（一覧の項目はすべて登録済みなので、
> トグルにしても状態を伝える意味が無く、操作を明示する方が分かりやすい）。
>
> 🎨 「未ログインでも星を出し、押したらログインへ誘導しては」との提案を採用。
> 隠すと機能の存在に気づけない。押した時点で「何をすれば使えるか」を伝え、
> ログイン後は元の画面に戻す（お気に入りにしたかった献立を探し直させない）。
> 未ログインでは API を呼ばない（押した時点で 401 と分かっているため）。
>
> 🐛 案内が星から離れて出ていた。星を包む箱が flex の既定（stretch）で
> カードの高さまで伸び、`top-full` の基準が箱の下端＝カード下端になっていた。
> `self-start` で箱を星の大きさに留めて解決。エラー表示も同じ影響を受けていた。
> jsdom にはレイアウト計算が無いため、この種のずれは単体テストでは検出できない
> （8-L の Playwright で拾える範囲）。
>
> 🎨 続けて「星の位置が献立の下だとカードが高くなる」との指摘。
> カードを横並びにして**献立の右端**に置いた。失敗時のメッセージは
> 絶対配置で重ねる（エラーのときだけ高さが変わって並びがずれるのを防ぐ）。
> 週間献立は7枚並ぶので、1枚あたりの高さが効いてくる。

### PR 8-M: ホーム画面 `feat/ui-home` ✅

- [x] 🔴 テスト: `/` がホーム画面で、各機能への入口が並ぶ
- [x] 🔴 テスト: 検索は `/search` に移動し、既存の導線が壊れていない
- [x] 🔴 テスト: ログイン状態でホームの表示が変わる（要ログインの断り）
- [x] 🟢 ホーム画面を実装し、検索を `/search` に移す
- [x] 🔧 実機確認（全ルートが 200）

> 認証が要る機能には、未ログインのとき**先に断りを出す**。押してから
> ログイン画面に飛ばされるより親切。
>
> ホームでもログイン判定が付くまでは状態に依る文言を出さない
> （8-I と同じ理由。決めつけると認証済みの利用者に一瞬ログインを勧める）。
>
> `/` の意味が変わったので、`MenuDetailPage` の直接アクセス時の戻り先を
> `/search` に、`NotFoundPage` の文言を「ホームへ」に直した。
> ヘッダにはアプリ名を置き、どの画面からでも起点に戻れるようにした。

> **8-I の動作確認から出た案。** 現状 `/` が検索画面のため、
> 「今どういう状態か」を示す場所がアプリに無い。ホームがあれば
> ログイン状態・各機能への入口・（ログイン中なら）直近の履歴などを
> 一望でき、ログアウトの前後で画面が明確に変わる。
>
> ただし**これは情報設計の変更**であって、ログアウトの分かりにくさへの
> 対処ではない（そちらは 8-I で「ログアウトしました」の表示として対処済み）。
> ルート構成・ヘッダ・E2Eシナリオに影響するため、独立したPRで行う。
>
> 影響範囲: `/` の意味が変わる（検索 → ホーム）ため、8-D/8-E のテストと
> `MenuDetailPage` の戻り先リンク、8-L のE2Eシナリオを見直す必要がある。
> **8-L（E2E）より前に入れる**こと。後に回すとE2Eを二度書くことになる。

### PR 8-L: E2E `feat/e2e` ✅

- [x] 🔧 Playwright を導入し CI に追加（stub プロバイダで実行）
- [x] 🔴 E2E: **和食×簡単 → 献立 → レシピ3件が新しいタブ**（PLAN.md の中核シナリオ）
- [x] 🔴 E2E: サインアップ → 検索 → 履歴に残る／履歴の削除／未ログインの誘導
- [x] 🔴 E2E: 週間献立の作成・1日だけ引き直し・遷移しても残る
- [x] 🔴 E2E: お気に入りの追加と一覧／未ログインでの誘導

> **E2E が単体テストでは取れない問題を3つ拾った。**
> 1. ラジオが `sr-only` で label に覆われており、input を直接クリックできない
>    （＝jsdom には当たり判定が無く、Playwright で初めて分かる）。
>    実利用者と同じく label を押すヘルパにした。
> 2. 週間献立の1項目に h2 が2つ（日付と献立名）あり、見出しの階層が読めない。
>    日付を `p` にして、読み上げは li の `aria-label` に任せる。
> 3. `getByRole('button', {name:'削除'})` が「すべて削除」にも一致する。
>    E2Eでは完全一致で指定する。
>
> 4. **詳細画面に h1 が無かった。** E2Eが時々落ちるので調べたところ、
>    「遷移後の h1 を待つ」つもりの検証が、実際には**遷移前の画面の h1**に
>    一致して通っていた（＝検証になっていなかった）。詳細画面では献立名を
>    h1 にし、テストも URL と見出しの中身で遷移完了を確かめるよう直した。
>
> Vitest の対象を `src/**` に限定した（`e2e/**` を拾うと
> `@playwright/test` の API が無くて落ちる）。

---

## フェーズ9: 仕上げ ✅

> 完了条件: E2E全通過 → 達成（CI 4ジョブ緑）
> **これをもってフェーズ0〜9すべて完了 = MVP 全機能の完成（2026-07-21）。**
> 本番デプロイ等の対象外項目は下記「積み残し / 将来対応」に整理済み。

### PR 9-0: 契約テスト `feat/contract-test` ✅

- [x] 🔴 テスト: 実レスポンスが `api/openapi.yaml` に適合する（9本）
- [x] 🟢 handler のテストに OpenAPI 検証を挟む
- [x] 🔧 故意に実装をズラして、検出できることを確認

> **kin-openapi ではなく libopenapi-validator を使った。** 仕様が
> OpenAPI 3.1 で、`type: 'null'` など 3.1 固有の記法を使っているため。
>
> **エラー経路は応答だけを検証する。** 不正なジャンルのような
> 「意図的に仕様違反のリクエスト」をリクエスト検証にかけても、
> 当然のことを言われるだけで意味がない。見たいのは「その入力に対する
> 応答の形」なので、`ValidateHttpResponse` を使う。
>
> ハンドラが本文を読み切っているため、検証前に巻き戻す必要がある
> （そのままだと「本文が空」と判定される）。

> 8-B2 でCIが守るのは「yaml と生成された型」の一致まで。yaml と Go 実装の
> ズレはここで塞ぐ。これが無いと、仕様を直し忘れたまま実装だけ変わっても
> フロントの型は古い仕様のまま通ってしまう。

### PR 9-A: レート制限 `feat/rate-limit` ✅

- [x] 🔴 テスト: 認証 10req/min/IP
- [x] 🔴 テスト: 検索 60req/min/IP
- [x] 🔴 テスト: 制限超過で 429
- [x] 🔴 テスト: IPごとに独立している
- [x] 🔴 テスト: 上限 0 以下は無制限（プロキシ配下で切るため）
- [x] 🟢 レート制限ミドルウェアを実装（IP単位の固定ウィンドウ。値で上限を受け取る）
- [x] 🔧 main.go で認証グループに 10/min、検索グループに 60/min を結線
- [x] 🔧 上限を環境変数で上書き可能に（`AUTH_RATE_LIMIT_PER_MIN` / `SEARCH_RATE_LIMIT_PER_MIN`）

> **Vite プロキシ配下では全リクエストが単一IPに集約される**（`xfwd` 既定 false で
> `X-Forwarded-For` を付けないため、backend が見る RemoteAddr はプロキシのIP）。
> spec 値のまま E2E を回すと 10req/min の認証制限に即達して落ちる。そこで上限を
> 環境変数化し、`docker-compose.yml`（開発・E2E）では 0（無制限）で切った。
> 本番は実クライアントIPが分かる構成で spec 値（10 / 60）を設定する。
> Playwright の2ワーカーも同一ホストIPになるため、`xfwd` 転送では回避できない。

### PR 9-B: ロギング `feat/logging` ✅

- [x] 🔴 テスト: リクエストIDが全ログに伝播する（下位層 context 経由 + アクセスログ）
- [x] 🔴 テスト: **パスワードがログに出ない**（本文を記録しない）
- [x] 🔴 テスト: **トークンがログに出ない**（Cookie / Authorization を記録しない）
- [x] 🟢 ロギングミドルウェアを実装（1リクエスト1行のアクセスログ）
- [x] 🔧 `internal/logctx` で request_id 付き logger を context に載せ、handler / service 両層に伝播
- [x] 🔧 既存の `slog.*` 呼び出し（menu.go / problem.go / service/menu.go）を request スコープ logger に載せ替え
- [x] 🔧 main.go で RequestID の後に結線

> **記録するのは method / path / status / latency / ip のみ。** 本文・機微ヘッダ
> （Cookie / Authorization）・クエリ文字列は出さない。クエリを外すのは OAuth の
> `code` / `state` が漏れないようにするため。
> **import 循環を避けるため logger の受け渡しは `internal/logctx`（中立パッケージ）** に置く。
> handler に置くと service（problem.go 経由で handler が service に依存）から参照できない。
> エラー時の status は ErrorHandler 確定前なので `toProblem(err).Status` で先取りして記録する。

### PR 9-C: 仕上げ

> 項目の毛色が異なるため「1振る舞い=1PR」に沿って複数PRに分割する。
> - 9-C-1 `feat/cors-restrict-origin`: CORS 検証（backend TDD）
> - 9-C-2 `feat/frontend-error-boundary`: エラーバウンダリ（frontend TDD）
> - 9-C-3 `docs/readme-finalize`: README・キャッシュ要否の判断記録・応答時間計測

#### 9-C-1: CORS 検証 `feat/cors-restrict-origin` ✅

- [x] 🔧 CORS が `FRONTEND_ORIGIN` のみ許可することの確認
- [x] 🔴 テスト: FRONTEND_ORIGIN には Allow-Origin + Allow-Credentials を返す
- [x] 🔴 テスト: 別オリジンには Allow-Origin を返さない
- [x] 🔴 テスト: プリフライト(OPTIONS)も同じ判定
- [x] 🟢 CORS 設定を `handler.CORS(origin)` に抽出（main.go はこれを使う）

#### 9-C-2: フロントのエラーバウンダリ `feat/frontend-error-boundary` ✅

- [x] 🔴 テスト: エラーが無ければ子をそのまま描画
- [x] 🔴 テスト: 子が描画時に throw したらフォールバック（role=alert）を表示
- [x] 🔴 テスト: フォールバックに再読み込み導線がある
- [x] 🔴 テスト: `fallback` prop を渡すとそれを表示
- [x] 🟢 `ErrorBoundary`（クラス）を実装。描画時エラーを捕捉し console.error に残す
- [x] 🔧 App の `<main>` を境界で包む。ヘッダは残し、`location.pathname` をキーに
      して別画面へ移ると自動で通常表示へ復帰する

> **error boundary はクラスでしか書けない**（描画中エラーは Hooks で捕捉不可）。
> **本文だけを包む**のは、ヘッダを残して他画面へ逃がすため。`key={pathname}` により
> ナビゲーションで境界が作り直され、エラー状態が自然にリセットされる。

#### 9-C-3: 仕上げドキュメント `docs/readme-finalize` ✅

- [x] 🔧 404画面（`NotFoundPage` + `path="*"`。PR 8系で実装済み）
- [x] 🔧 README（概要・技術スタック・アーキテクチャ図(mermaid)・起動手順・環境変数・開発・構成・性能）
- [x] 🔧 `.env.example` にレート制限の env を追記（9-A で追加した分が未記載だった）
- [x] 🔧 `recipe_link_caches` の要否を判断
      → **spec.md 13.2 で導入決定済み（2026-07-17）**。献立120件固定のため生涯約120クエリで
        頭打ちになり、外部API消費を抑えられる。判断は済んでおり README にも結論を明記した。
- [x] 🔧 応答時間の計測（検索 p95 200ms以内）
      → **達成**。docker compose（stub・シード済み）で backend へ逐次 curl した実測:
        `GET /menus/suggest` p95 **3.0ms**、`GET /menus/:id` p95 3.0ms、
        `POST /menus/suggest-weekly` p95 3.6ms。いずれもDBのみで完結し目標200msを大きく下回る。

---

## フェーズ10: 本番デプロイ（ハイブリッド）✅

> **2026-07-21 本番公開。全機能が本番で稼働中**（メール/パスワード認証・Google SSO・
> Brave 実キーでのレシピ取得・レート制限まで検証済み。結果は DEPLOY.md に記録）。
>
> 構成: Cloudflare Pages（フロント＋/apiプロキシ＝同一オリジン）＋ Cloud Run（Goコンテナ）＋ Neon（Postgres）。
> 手順の詳細は [`DEPLOY.md`](./DEPLOY.md)。backendホストの既定は Cloud Run（Fly.io に差し替え可）。

#### 10-A: デプロイ手順書 `docs/deploy-runbook`

- [x] 🔧 DEPLOY.md（構成図・アカウント・環境変数マトリクス・手順・ロールバック）
- [x] 🔧 task.md にフェーズ10の枠を追加

#### 10-B: Pages の /api プロキシ `feat/pages-api-proxy` ✅

- [x] 🔧 Pages Function（`functions/api/[[path]].ts`）で `/api/*` を backend(`BACKEND_ORIGIN`) に転送
- [x] 🔧 `CF-Connecting-IP` を `X-Forwarded-For` / `X-Real-IP` として前送り（実IPをbackendへ）
- [x] 🔴 テスト: パス・クエリ保持／実IP付与／Host非転送／Cookie透過／POST本文／
      redirect=manual／応答(Set-Cookie)透過／末尾スラッシュ正規化（8本）
- [x] 🟢 中核ロジックを `src/server/apiProxy.ts` の純関数に切り出し

> **`Host` を転送しない**のが要点。Cloud Run は Host でサービスを判別するため、
> フロントの Host を送ると宛先を見失って 404 になる。削除して fetch に組み立てさせる。
> **redirect は `manual`**。Google SSO の 302 をプロキシが追うとログインが完了しない。
> ロジックを `src/` の純関数に置いたのは、`vitest` の対象が `src/**` に限られ、
> `tsc -b` の検査対象も `src` のため。`functions/` は薄いグルーに留める。

#### 10-C: 実IPでのレート制限 `feat/trust-proxy-real-ip` ✅

- [x] 🔴 テスト: 共有シークレット一致時は `X-Forwarded-For` 先頭の実クライアントIPを使う
- [x] 🔴 テスト: シークレット無し／不一致の直リクエストは偽装ヘッダを無視し接続元を使う
- [x] 🔴 テスト: シークレット未設定なら常に接続元（安全側の既定）
- [x] 🔴 テスト: `X-Real-IP` へのフォールバック／XFF 複数要素の先頭採用
- [x] 🔴 テスト: 同一プロキシ経由でも実クライアントごとに別カウント（レート制限と結合）
- [x] 🔴 テスト(front): シークレット送信／未設定時は送らない／クライアント詐称値は握り潰す
- [x] 🟢 `handler.TrustedProxyIPExtractor` を実装し `e.IPExtractor` に設定
- [x] 🟢 Pages Function が `X-Proxy-Secret` を付与
- [x] 🔧 本番の環境変数を設定（Cloud Run: `TRUSTED_PROXY_SECRET`・レート制限 10/60、
      Pages: `TRUSTED_PROXY_SECRET` 同値）※デプロイ作業（2026-07-22 完了・外形検証済み）
      → 検索 60/min（60×200 → 429）、認証 10/min（10×401 → 429）を実測。
        詐称防止も確認: 公開ドメイン経由では Function がクライアント送信の
        `X-Forwarded-For` を握り潰し、backend 直叩きでは秘密なしの XFF を信頼せず
        接続元IPで計数（偽XFFを変えても同一バケツのまま 429）。

> **ホップ数ではなく共有シークレットで信頼を判定する。** backend のURLは公開されており
> 直接叩けるため、XFF を無条件に信じると毎回違うIPを詐称するだけでレート制限を回避できる。
> ホップ数指定でも、直リクエストに偽の XFF を並べられると同じ穴が残る。
> 前段だけが知る秘密で「自分のプロキシ経由」を確かめる方式にした。
> 比較は定数時間（`crypto/subtle`）。秘密未設定時は転送ヘッダを一切信頼しない。

#### 10-D: 本番ハードニング `feat/prod-hardening` ✅

- [x] 🔧 本番 CORS（同一オリジン構成のためブラウザは実質使わない。`FRONTEND_ORIGIN` のみ許可を確認）
- [x] 🔧 Cookie の Secure が本番で効くこと → `session.go` で `HttpOnly`＋`Secure`＋`SameSite=Lax` 固定。
      本番HTTPSで有効、localhost はブラウザ例外で開発も動く。**変更不要**
- [x] 🔧 Google OAuth の本番リダイレクトURL（Pages経由）を確認 → `/api/v1/auth/google` が
      302 で accounts.google.com へ。同一オリジンのプロキシ経由でOAuth開始が通る
- [x] 🟢 `FRONTEND_ORIGIN` 未設定時に起動警告を出す
- [x] 🔧 本番の実測値と稼働確認の結果を DEPLOY.md に記録

> **`FRONTEND_ORIGIN` は CORS だけでなく Google ログイン完了後の戻り先**でもある
> （`google.go`）。未設定だと既定の localhost に飛び、「認証は成功するのに
> 利用者が localhost へ飛ばされる」という気付きにくい壊れ方をする。
> 外形からは検知できないため、既定値で起動したことを警告に残すようにした。

#### 10-E（任意）: 自動デプロイ ✅

- [x] 🔧 main マージで backend(Cloud Run)・frontend(Pages) を自動デプロイ
      → **GitHub Actions は不要だった**。Cloud Run の継続的デプロイ（Cloud Build）と
        Cloudflare Pages の Git 連携で実現済み。#75/#76 のマージ後、約2分半で
        backend が自動再デプロイされることを実測（未定義パスが 401→404 に切り替わった）。
        Pages は PR ごとにプレビュービルドも走る。

---

## 本番稼働後に見つかった不具合

### Issue #78: アカウント切り替えで前ユーザーのデータが表示される ✅

利用者からの報告。Aでログイン→お気に入り画面→ログアウト→Bでログインすると、
Aのお気に入りが表示されたままになる（履歴も同じ）。

- [x] 🔴 テスト: Aのお気に入りを見た後にBでログインするとBのものだけが出る
- [x] 🔴 テスト: 履歴についても同様
- [x] 🟢 ログアウト時に `queryClient.resetQueries()`（全クエリを初期化）
- [x] 🟢 ログイン時に `queryClient.clear()` してから新ユーザーを入れる

> **サーバは正しく、漏れていたのはブラウザ内のキャッシュ**。`['me']` だけ捨てて
> ユーザー固有の `favorites` / `histories` を残していた。`staleTime: 30_000` のため
> 取り直しも走らず、古いデータが描画され続けていた。
>
> **消すキーを列挙する方式は採らない。** ユーザー固有のクエリを増やしたときに
> 消し忘れて静かに漏れるため、丸ごと初期化する。
>
> **ログアウトは `clear()` ではなく `resetQueries()`。** `clear()` は表示中のクエリを
> 取り直さないため、守られた画面でログアウトすると「読み込み中…」で固まる
> （実装中に実際に踏んだ）。ログイン側は直後に `setQueryData` と画面遷移が
> あるため `clear()` で問題ない。

### Issue #81: 画面遷移でエラーになると生の problem+json が表示される ✅

利用者からの報告。レート制限を超えた状態で「Googleでログイン」を押すと、
`{"type":"...rate-limited","title":"リクエストが多すぎます",...}` が画面にそのまま出る。

- [x] 🔴 テスト: `Accept: text/html` のエラーは 302 で `/login?error=種別` へ
- [x] 🔴 テスト: `*/*` / Accept無し / `application/json` は従来どおり problem+json
- [x] 🔴 テスト(front): `?error=` の種別ごとに利用者向けの案内を出す／未知の種別でも黙らない
- [x] 🟢 `ErrorHandler` に内容交渉を追加
- [x] 🟢 `LoginPage` が `?error=` を文言に翻訳して表示

> **原因は「fetch ではなくブラウザの全ページ遷移」でAPIを叩く経路があること。**
> OAuth の開始にはトップレベル遷移が必要なので `href="/api/v1/auth/google"` 自体は妥当。
> だがエラー応答が problem+json だとブラウザが本文をそのまま描画してしまう。
> 429 に限らず、Google認証の失敗（state不一致など）やコールバックでも同じだった。
>
> **相対パスでリダイレクトする**のが要点。フロントと backend は同一オリジンで
> 配信されるため、`/login?error=...` を返せばブラウザがフロントのオリジンに解決する。
> ErrorHandler にフロントのURLを渡す必要がなく、シグネチャを変えずに済んだ。
>
> 判定は `Accept` に `text/html` を含むか。アプリ内の fetch は `*/*` を送るため
> 従来どおり problem+json を受け取り、契約テストにも影響しない。

### Issue #83: 週間献立がログアウト・アカウント切り替え後も残る ✅

利用者からの報告。#78 を直した後も、1週間分の献立だけは残り続けていた。

- [x] 🔴 テスト: 保存した値が作り直しても復元される（既存の振る舞いを壊さない）
- [x] 🔴 テスト: `clearSessionState()` で保存値が消える／マウント中の値も初期値に戻る
- [x] 🔴 テスト: アプリ以外のキーは消さない
- [x] 🔴 テスト: ログアウトすると週間献立がその場で消える（App 経由の統合テスト）
- [x] 🟢 `useSessionState` にキー接頭辞と `clearSessionState()` を追加
- [x] 🟢 ログアウト／ログインの2箇所で呼ぶ（#78 と同じ場所）

> **#78 と同じ種類の漏れだが経路が違う。** 週間献立は「レシピを見て戻ると消える」問題を
> 直したときに **sessionStorage** に持たせた（`fix/weekly-state-persistence`）。
> #78 で消したのは React Query のキャッシュだけなので、こちらは残っていた。
>
> **保存を消すだけでは足りない。** `useSessionState` は値を `useState` にも持つため、
> 表示中の画面には残ってしまう（別画面へ移って戻れば消える、という中途半端な状態）。
> 削除時にイベントを飛ばし、マウント中のフックも初期値へ戻す。
>
> `sessionStorage` を無差別に消さず、**接頭辞 `menu-planner:` の付いたキーだけ**を対象にする。

---

## フェーズ11: 必要食材と買い物リスト

> 仕様は spec.md 2.7（機能）／4.2（テーブル）／5.5（API）／**14章（決定事項）**。
> 積み残しの「必要食材の取得・表示」と「買い物リスト生成」の2件が対象。

**着手前に決めたこと（spec.md 14章）**
- 食材は**自前で持つ**。レシピのクロールは不可（1.1）、レシピAPIは自前の献立名と対応づかない
- **分量は持たない**。買い物リストは食材名のチェックリストとして成立する。
  分量が要るのは栄養価計算だけで、それは着手順の最後
- 補償として買い物リストに**「その食材をどの献立で使うか」を併記**する
- 除外機能は将来「**苦手食材**」として作る。アレルギー対応とは謳わない（14.3）
- **調味料は登録しない。食材のみ**（14.4）。境界は「その献立のために買い足すか」で判断し、
  迷ったら登録しない側に倒す。常備品フラグのような仕掛けも不要になった

#### 11-A: 仕様の確定 `docs/spec-shopping-list` ✅

- [x] 🔧 spec.md 2.7 / 4.1・4.2 / 5.5 / 10章 / 14章 を追加
- [x] 🔧 task.md にフェーズ11のPR分割

#### 11-B: 食材のドメインとスキーマ `feat/ingredient-schema` ✅

- [x] 🔴 テスト: `domain.Ingredient` の検証（ID未設定・名前/カナが空白のみ・名前が長すぎ・不正カテゴリ）
- [x] 🔴 テスト: カテゴリの並び順（野菜→肉→魚介→卵乳→主食→その他）
- [x] 🔴 テスト: `seasoning` は拒否される（14.4 をドメインでも守る）
- [x] 🔴 テスト: `IngredientID` はゼロ値UUIDを受け付けない（MenuID と同じ方針）
- [x] 🟢 `domain.Ingredient` / `IngredientCategory` / `IngredientID` を実装
- [x] 🔧 マイグレーション `000007_create_ingredients`
- [x] 🔴 統合テスト: CHECK（不正カテゴリ・`seasoning`・空白名/カナ）・名前UNIQUE・
      複合主キーの重複拒否・献立削除でCASCADE・使用中食材はRESTRICTで消せない

> **`seasoning` はDBのCHECK制約でも弾く。** アプリ側だけの防御だと、手で流すSQLや
> シード修正で誤って入り込む。14.4 の判断をスキーマにも刻む。
> **`ON DELETE RESTRICT`** は、使われている食材を消せてしまうと献立から食材が
> 黙って欠落するため。献立側は `CASCADE`（献立を消せば紐付けも不要）。

#### 11-C: 食材シードデータ `feat/ingredient-seed` ✅

- [x] 🔧 献立120件分の食材データを作る → **食材131種・紐付け556件・平均4.6種/献立**
- [x] 🔧 シード投入を3段（献立→食材→紐付け）に拡張。1トランザクションで巻き戻す
- [x] 🔴 テスト: 全献立が1件以上の食材を持つ（取りこぼし検出）
- [x] 🔴 テスト: どの献立からも使われていない食材が無い（綴り違い・消し忘れ検出）
- [x] 🔴 テスト: 調味料・粉類が名前として紛れ込んでいない
- [x] 🔴 テスト: シードが冪等（再実行で0件・行数不変）

> **紐付けは名前で結合する。** menus / ingredients とも id は `gen_random_uuid()` で
> 採番するためIDを直書きできない。名前にはどちらも UNIQUE があるので一意に定まる。
> ただし**綴りを1文字間違えると JOIN が外れて黙って0件になる**ため、
> 「食材の無い献立」「使われていない食材」の両方をテストで検出する。
>
> **投入順に意味がある**（献立・食材が入る前に紐付けを流すと0件になる）ため、
> 3段を1トランザクションにまとめ、途中で失敗したら巻き戻す。
>
> データ整備の方針: 肉の粒度は中程度（豚こま切れ肉・鶏もも肉など）、
> 肉じゃがは豚（関東風・流通量が多い）、主菜の材料のみ。
> かぼちゃの煮物が1種、いなり寿司・鰻の蒲焼きが2種と少ないのは、
> 調味料を除いた結果であってデータ誤りではない。

#### 11-C2: シードを upsert にする `fix/seed-upsert` ✅

「途中で食材やレシピを足しても問題ない作りか」という問いを検証した結果、
**追加は安全だが「修正」が反映されない**ことが分かったので直す。

- [x] 🔴 テスト: 既存行の内容を壊して再シードすると正しい値に戻る（食材のカナ・カテゴリ、献立の説明）
- [x] 🟢 `menus.sql` / `ingredients.sql` を `DO NOTHING` → `DO UPDATE`（upsert）に変更
- [x] 🔧 冪等性テストの判定を「影響行数ゼロ」→「行数が変わらない」に修正
      （upsert では既存行の上書きも影響行数に数えられるため）

> **検証で分かったこと（実験済み）**
> - 献立・食材・紐付けの**追加**は再シードで反映される（紐付けを消して再シードしたら復旧を確認）
> - **既存行の修正は反映されなかった**。玉ねぎのカナを `まちがい` に変えて再シードしても直らない。
>   カナは買い物リストの並び順、カテゴリは売り場の分類に使うため実害になる
> - 食材の**削除**は使用中だと RESTRICT で拒否される（設計通り）
> - **名前の変更は upsert でも直らない**（name がキーのため旧行が残る）。マイグレーションが要る
> - **紐付けの削除も反映されない**（挿入のみ）。「この食材は要らなかった」はマイグレーションで対応
>
> シードファイルを「あるべき状態の定義」として扱う方針に寄せた。

#### 11-D: 食材の取得 `feat/ingredient-repository` ✅

- [x] 🔴 統合テスト: 献立IDから食材を取得（カナ順で安定）
- [x] 🔴 統合テスト: 複数の献立IDから、食材と「使う献立」の対応をまとめて取得
- [x] 🔴 統合テスト: 同じ食材が複数の献立に使われていれば、その数だけ対応が返る
- [x] 🔴 統合テスト: 存在しない献立IDは黙って除く（エラーにしない）
- [x] 🔴 統合テスト: 空指定は空を返す／重複IDでも二重に返らない
- [x] 🟢 `service.IngredientRepository`（ポート）と `repository.IngredientRepository`（実装）

> **メソッドは1つだけにした。** 1献立用と複数献立用を分けず、`FindByMenuIDs` に集約する。
> 単一献立は要素1つで呼べばよく、同じSQLを二重に持たずに済む。
>
> **カテゴリ順の並べ替えはSQLでやらない。** 表示順は `domain.AllIngredientCategories` が持つ
> 知識で、SQLに CASE で書き写すと定義が二重になって食い違う。
> repository はカナ順で安定させ、カテゴリ順は service が担う。
>
> **カテゴリはDBのCHECKに加えて Go 側でも解釈して弾く。** 制約を後から緩めたときに、
> 上位へ黙って不正な値が流れるのを防ぐ。

#### 11-E: 買い物リストの組み立て `feat/shopping-list-service` ✅

- [x] 🔴 テスト: 同じ食材が複数の献立に出たら1件にまとまり、`usedIn` に両方が入る
- [x] 🔴 テスト: 並び順（カテゴリ順 → カナ順）
- [x] 🔴 テスト: 重複した献立IDは1件として扱う（`usedIn` も重複しない）
- [x] 🔴 テスト: 0件・8件以上は不正（`ErrInvalidMenuIDs`）
- [x] 🔴 テスト: 存在しない献立IDが混ざれば `ErrMenuNotFoundInList`
- [x] 🔴 テスト: repository への問い合わせは1回にまとめる（7日分で7往復させない）
- [x] 🔴 テスト: repository の失敗を伝播する
- [x] 🟢 `service.ShoppingListService` を実装

> **存在しない献立が混ざったら 404 にする（黙って無視しない）。**
> 無視すると「頼んだ献立の食材が抜けたリスト」を渡してしまい、
> 利用者は買い忘れに気づけない。件数が合わなければエラーにする。
>
> **並び順は service で決める。** repository はカナ順で返すため安定ソートなら
> カテゴリだけ指定すれば足りるが、**並び順を repository の実装に依存させない**よう
> ここで (カテゴリ, カナ) の両方を指定する。
>
> 調味料はそもそも登録されていない（14.4）ので、リストは常に買う食材だけになる。
> service 側で除外する処理は不要（テストも不要になった）。

#### 11-F: API `feat/api-shopping-list` ✅

- [x] 🔴 テスト: `GET /menus/:id/ingredients` が200／0件は`[]`／不正UUIDで400（serviceを呼ばない）／存在しないIDで404
- [x] 🔴 テスト: `POST /shopping-list` が200・`usedIn`つき／0件・上限超過で400／存在しないIDで404／
      壊れた本文で400／未知のエラーは500で詳細を漏らさない
- [x] 🟢 `ShoppingListHandler` / `IngredientHandler` を実装し `/api/v1` に結線（検索系と同じレート制限）
- [x] 🟢 `service.MenuIngredients` を追加（献立の存在を先に確認する）
- [x] 🔧 `api/openapi.yaml` に2経路と4スキーマを追加し、TSの型を再生成
- [x] 🔧 契約テストに3本追加（食材／買い物リスト／400のproblem）
- [x] 🔧 実機確認: 親子丼→カテゴリ順で食材、2献立の統合、0件400・存在しない404

> **献立の存在を先に確かめる。** 確かめないと、存在しない献立でも「食材0件」の200を
> 返してしまい、利用者は「食材が登録されていない献立」と区別できない。
>
> **買い物リストと食材は同じ service が担う**（どちらも献立×食材を扱う）。
> ハンドラは用途ごとに分け、インターフェースも別にした（HTTP境界の関心が違うため）。

#### 11-G: フロントエンド `feat/ui-shopping-list` ✅

- [x] 🔴 テスト(食材): カテゴリ見出しごとに並ぶ／該当カテゴリの見出しだけ出す／
      注意書きが出る／未登録の案内／取得失敗でエラー表示
- [x] 🔴 テスト(買い物リスト): `usedIn` 併記／カテゴリ見出し／週間献立が無ければ導線つきで案内／
      注意書き／取得失敗でエラー表示
- [x] 🔴 テスト(週間献立): 作る前はリンクを出さず、作ったら「買い物リストを見る」が出る
- [x] 🟢 `IngredientList`（献立詳細に組み込み）／`ShoppingListPage`（`/shopping-list`）を実装
- [x] 🔧 マスコット配色（`kon-*`）と共通コンポーネント（`MascotEmpty` / `MascotStatus`）に揃えた
- [x] 🔧 実機確認: 週間献立7日分 → 食材29種がカテゴリ順・カナ順・`usedIn` 集約つきで出る

> **並び順はフロント側でも持つ。** サーバも同じ順で返すが、
> **並び順をサーバの実装に依存させない**ため `categoryOrder` を画面側にも置く。
>
> **週間献立は sessionStorage から読む。** サーバに保存していないため、
> `WeeklyPage` が置いているキー（`weekly.week`）を共有する。
> 週が無ければ問い合わせず、先に週間献立を作る導線を出す（0件はサーバが400にするため）。
>
> **注意書きは食材が出る画面すべてに置く。** 代表的な食材の例であって正確な材料表では
> ないため（14.1）。アレルギー対応と誤解されると実際に危害が及びうる。

#### 11-H: 仕上げ `feat/shopping-list-e2e` ✅

- [x] 🔧 E2E: 週間献立を作る → 買い物リストを開く → 食材が並ぶ（カテゴリ見出し・注意書きも）
- [x] 🔧 E2E: 週間献立が無ければ導線つきで案内する
- [x] 🔧 E2E: 検索した献立にそのまま必要な食材が出る
- [x] 🔴 テスト: 検索画面にも食材が出る（下記の抜けを塞ぐ）
- [x] 🟢 `SearchPage` に `IngredientList` を追加
- [x] 🔧 MSW の既定ハンドラに `/menus/:id/ingredients` を追加
- [x] 🔧 README に機能を追記

> **E2E を書いて導線の抜けに気づいた。** 食材を出していたのは献立詳細だけで、
> そこへ行けるのは履歴・お気に入り・週間献立から。**アプリの中核である検索画面からは
> 献立詳細に行けず、食材が見えなかった**。「何を買えばいいか」は結果を見た直後に
> 知りたい情報なので、検索画面にも出すようにした。
> 単体テストは画面ごとに閉じて書くため、この抜けは通しで動かすまで見えなかった。
>
> **既定ハンドラを足す必要があった。** 食材は献立を出す画面すべてが引くため、
> MSW の既定が無いと個々のテストが未処理リクエストで落ちる。

---

## フェーズ12: 週間献立の保存

> 利用者レビューの項目 F（下記「利用者レビューからの後続タスク」）。
> **「いざ買い物に行ったときにすぐ見られる」が動機**なので、別デバイス・後日でも
> 開けることが要件。現在は sessionStorage のみでタブを閉じると消える（Issue #83）。
>
> 仕様は spec.md 2.8 / 4.2 / 5.3。**最優先だった C（日常性）ではなくこちらを先にやるのは、
> C が献立マスタの拡張（`feature/menu-master-expansion`）と `db/seeds/menus.sql` で
> 正面衝突するため。** マスタが確定してから C に入る。
>
> ブランチ: `feature/weekly-menu-save`。マイグレーション番号は **000008 を予約**した。

### 設計の要点（着手前に決めたこと）

- **保存の上限は10件で、超過は 409。** 履歴のように FIFO で押し出さない。
  保存は利用者の明示的な操作なので、黙って消えると行為の意味が壊れる（spec.md 2.8）
- **名前は付けない。** 保存日時と献立名で識別できる。命名必須は
  「買い物前にさっと取っておく」動機に対して重い
- **買い物リストは保存しない。** 開くたびに作り直す。食材は献立マスタ由来で不変
- **「開く」にAPIは要らない。** 一覧の応答に7日分を含めるため、
  クライアントが作業中の状態に戻すだけで済む

> **買い物リスト画面は無改修で済む見込み。** `ShoppingListPage` は
> sessionStorage の `weekly.week` を読んでいる（`useSessionState`）。
> 「開く」がそこへ書き戻す実装なら、買い物リストも引き直しもそのまま動く。
> **この見込みが外れたら設計を見直すこと。**

#### 12-A: 仕様 `docs/weekly-menu-save-spec`

- [x] 🔧 spec.md 2.8（機能要件）・4.2（テーブル2つ）・5.3（API 3本）・1.2（スコープ）
- [x] 🔧 task.md にフェーズ12の枠

#### 12-B: スキーマとリポジトリ `feat/weekly-menu-schema`

- [ ] 🔴 テスト: 保存・取得で7日分が往復する
- [ ] 🔴 テスト: 同じ週に同じ `day` を2件入れられない（複合PK）
- [ ] 🔴 テスト: ユーザー削除で保存も消える（CASCADE）
- [ ] 🔴 テスト: 一覧は新しい順
- [ ] 🟢 マイグレーション `000008_create_saved_weekly_menus`
- [ ] 🟢 `SavedWeeklyMenuRepository`（保存・一覧・削除・件数）

#### 12-C: 保存API `feat/api-save-weekly-menu`

- [x] 🔴 テスト: 7日分ちょうどで 201
- [x] 🔴 テスト: 過不足は 400（0日・6日・8日）
- [x] 🔴 テスト: `day` の重複・範囲外（0・8・-1）は 400
- [x] 🔴 テスト: 存在しない `menuId` は **404**（当初 400 と書いていたのを訂正）
- [x] 🔴 テスト: 上限に達していたら 409。上限の1つ手前は通る（境界）
- [x] 🔴 テスト: 未認証は 401。壊れたJSONは 400
- [x] 🔴 テスト: 件数の取得に失敗したら保存しない
- [x] 🟢 `POST /weekly-menus`（service / handler / problem 変換 / main.go 結線）
- [x] 🔧 `api/openapi.yaml` に経路を追加し `make gen-api`

> **存在しない献立を 400 ではなく 404 にした。** 着手時は 400 と書いていたが、
> 既存の扱いに合わせた。お気に入りの追加（`ErrMenuNotFound` → 404）も、
> 買い物リストの `ErrMenuNotFoundInList` → 404 も、「参照先が無い」ことを
> 404 で伝えている。ここだけ 400 にすると同じ状況に別の答えを返すことになる。
>
> **上限の判定は数えてから書くまでの間に割り込まれうる。** 同一利用者が
> 同時に保存を送ると11件目が通る余地がある。上限は課金や権限の境目ではなく
> 「際限なく溜めない」ための目安なので、DB制約（件数を数えるトリガや排他ロック）
> までは要らないと判断した。判断の根拠は service のコメントに残してある。

#### 12-D: 一覧・削除API `feat/api-list-delete-weekly-menu`

- [x] 🔴 テスト: 一覧は中身の7日分を含む・0件でも `null` ではなく `[]`
- [x] 🔴 テスト: 他人の保存は見えない（12-B のリポジトリ側で担保）
- [x] 🔴 テスト: 削除で 204／他人のものは 404／壊れたIDは 400
- [x] 🔴 テスト: 一覧・削除とも未認証は 401（service を呼ばない）
- [x] 🟢 `GET /weekly-menus` / `DELETE /weekly-menus/:id`
- [x] 🔧 `api/openapi.yaml` に2経路と `SavedWeeklyMenu` スキーマを追加し `make gen-api`

> **一覧の1日分は週間献立の提案と同じ `DayMenu` を使う。** 画面では
> 「作ったばかりの週」と「保存した週」を同じものとして扱えた方が素直で、
> 12-F の「開く」もそのまま作業中の状態に戻すだけで済む。

#### 12-E: 保存のUI `feat/ui-save-weekly-menu`

- [x] 🔴 テスト: 週が出来ていないときは保存ボタンもログイン導線も出さない
- [x] 🔴 テスト: 未認証では保存ボタンの代わりに `/login` への導線を出す
- [x] 🔴 テスト: ログイン済みなら7日分を送って保存し、知らせを出す
- [x] 🔴 テスト: 409 は「10件まで」「削除してください」と伝える
- [x] 🔴 テスト: 引き直すと保存済みの知らせを消す
- [x] 🟢 `WeeklyPage` に保存ボタンと `saveWeeklyMenu`

> **未認証では押せないボタンを出さない。** 押しても 401 になるだけなので、
> 何をすれば保存できるか（ログイン）を示す方が短い。

> **引き直したら保存済みの知らせを消す。** 出したままだと
> 「いま画面に出ている週が保存されている」と誤解させる。実際に保存されているのは
> 引き直す前の状態。新しく週を作り直したときも同様に消す。

> **409 だけ文言を差し替える。** 上限では押し直しても直らず、次に取るべき行動は
> 「古いものを消す」であって「もう一度試す」ではない。サーバの title をそのまま
> 出すと、その行動が伝わらない。

#### 12-F: 保存一覧の画面 `feat/ui-saved-weekly-list`

- [x] 🔴 テスト: 保存日時と中身の一部（先頭3件＋残り件数）を並べる
- [x] 🔴 テスト: 0件なら作って保存するよう促す
- [x] 🔴 テスト: 「開く」で週間献立画面に移り、その週が出ている
- [x] 🔴 テスト: 開いても前回の絞り込み条件は引き継がない
- [x] 🔴 テスト: **本物の `WeeklyPage` で7日分・引き直し・買い物リスト導線が揃う**
- [x] 🔴 テスト: 削除で一覧から消える／削除に失敗しても一覧は残る
- [x] 🟢 `SavedWeeklyPage` とルーティング（認証必須）・ヘッダ導線

> **12-A の見込みが実物で裏取りできた。** 「開く」は sessionStorage の
> `weekly.week` に書き戻すだけで、`WeeklyPage` も `ShoppingListPage` も無改修。
> スタブではなく本物の `WeeklyPage` に載せ替えたテストで確認している。

> **絞り込み条件は保存していないので、開くときに空へ戻す。** 引き継ぐと
> この週とは無関係な条件で引き直すことになる。

> **一覧には中身を先頭3件だけ出す。** 7件すべて並べると縦に伸びて選びにくい。
> 名前を付けない仕様のため、保存日時と献立名の並びが識別の手がかりになる。

#### 12-G: E2E `feat/e2e-weekly-menu-save`

- [x] 🔴 作る→保存→**sessionStorage を消す**→一覧から開く→献立名が一致→買い物リストが出る
- [x] 🔴 未ログインでは保存ボタンが無く、ログイン導線からログイン画面へ行ける
- [x] 🔴 保存した週を削除できる
- [x] 🔴 保存は本人のものだけが見える（別ユーザーでは0件）
- [x] 🔧 README の概要・構成図・設計判断・対象外の記述を更新

> **sessionStorage を消してから開き直す。** 消さずに遷移するだけだと、
> サーバに保存できていなくても「残っている」ように見えてしまい、
> このフェーズの要件（端末・セッションをまたいで見られる）を検証したことにならない。

> **保存前の献立名7件を控え、開き直した後と突き合わせる。** 件数だけ見ても
> 「別の週が7件出ている」状態と区別が付かない。

---

## フェーズ13: 手持ちの食材から献立を探す

> 積み残しの「手持ちの食材から献立を検索」。利用者レビューでも
> 「冷蔵庫にある材料で何が作れるのか知れたら最高です」と挙がっていた。
>
> 仕様は spec.md 2.9 / 5.6。**マイグレーションは不要** — 既存の
> `ingredients`（166種）と `menu_ingredients`（1,577件）だけで組める。
> フェーズ11 でこのデータを整備したことがそのまま効いている。
>
> ブランチ: `feature/search-by-ingredients`。

### 着手前に決めたこと（2026-07-22）

3点とも利用者の判断を仰いだ。いずれも後から変えると作り直しになる箇所。

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| 一致の基準 | **不足を許し、足りない食材を示す** | 献立1件の食材は平均4.4種。完全一致に絞ると候補がほぼ0件になり機能が成立しない |
| 入力方法 | **カテゴリ別の一覧から選ぶ** | 自前マスタなので自由入力だと表記揺れの吸収が要る。一致しない理由を利用者に説明できない |
| 結果の形 | **複数を一覧で** | 「この食材で何が作れるか」は選びたい問い。1件ずつ引く既存の検索とは別の体験 |

**並び順は「不足の少ない順 → 一致の多い順 → カナ順」。** 第1が不足なのは、
知りたいのが「いま作れるか」だから。同じ不足数なら手持ちを多く使う方を上に出す。

**上位20件まで。** 食材を多く選ぶほど候補は増えるが、見比べる用途で20件を超えても選べない。

**履歴には記録しない。** 候補を並べただけでは献立が確定していない（週間献立の引き直しと同じ）。

#### 13-A: 仕様 `docs/search-by-ingredients-spec`

- [x] 🔧 spec.md 2.9（機能要件）・5.6（API 2本）・1.2（スコープ）
- [x] 🔧 task.md にフェーズ13の枠。積み残しの該当項目に着手を記録

#### 13-B: 食材一覧API `feat/api-list-ingredients`

- [x] 🔴 テスト（repository/service/handler 計11本）: カナ順で全件／カテゴリ順に並ぶ／未認証で 200／0件は `[]`
- [x] 🟢 `GET /ingredients`（`FindAll` / `IngredientService` / handler / main.go 結線）
- [x] 🔧 `api/openapi.yaml` に経路を追加し `make gen-api`

> **`IngredientService` を新設した。** 買い物リストが「献立×食材」を扱うのに対し、
> こちらは献立に紐づかない食材の一覧を担う。
> カテゴリ順→カナ順の比較関数が既に2箇所にあり3箇所目になるところだったので
> `sortIngredientsForDisplay` に切り出した。

> **テスト用DBの後始末が `ingredients` を消していない**（`menus` を参照していないため
> CASCADE が届かない）。全件を数えるテストは `clearIngredients` で明示的に空にする。

> 入力の選択肢に要る。現状 `IngredientRepository` は `FindByMenuIDs` しか持たず、
> **食材マスタを一覧するAPIが存在しない。**

#### 13-C: 食材から検索API `feat/api-search-by-ingredients`

- [x] 🔴 テスト（repository/service/handler 計19本）: `matched`/`missing` の中身／
      並び順／重ならない献立は返らない／0件 400・重複ID・存在しないID 404／上位20件で打ち切り
- [x] 🟢 `POST /menus/search-by-ingredients`
- [x] 🔧 `api/openapi.yaml` に経路と `MenuMatch` スキーマを追加し `make gen-api`

> **実データで裏付けが取れた。** 献立360件・食材166種に対し
> 「玉ねぎ・じゃがいも・にんじん」で検索すると候補20件（上限）で、
> **不足0の献立は1件も無かった。** 完全一致に絞らない判断が正しかったことになる。
> 「肉じゃが 要: 豚こま切れ肉・糸こんにゃく」のように出る。

> **`domain.ErrInvalidIngredientID` の problem 写像が無かった**（この経路で初めて
> 本文由来の食材IDを解釈するようになった）。無いと壊れたIDが 500 になる。
> フェーズ12でも同型の見落とし（保存IDの写像欠落）があり、2度目。

> **⚠️ マージ順の事故があった。** 13-C を 13-B のブランチに積んだため、
> 13-B が先に feature へマージされた時点で 13-C の行き先が行き止まりになり、
> 本流に届かなかった（PR #124 で回収）。**各PRのCIは緑なので検知できない。**
> このフェーズでは以降PRを積まず、前のPRがマージされてから次を切る。

#### 13-D: 食材を選ぶUI `feat/ui-ingredient-picker`

- [x] 🔴 テスト: カテゴリごとに分かれて並ぶ／該当が無いカテゴリの見出しは出さない
- [x] 🔴 テスト: 選択・解除が同じ経路（`onToggle`）を通る／選択中の件数を伝える
- [x] 🔴 テスト: 1つも選んでいなければ何をすればよいか示し、解除の導線は出さない
- [x] 🔴 テスト: まとめて外せる
- [x] 🟢 `IngredientPicker`

> **状態は持たせず、選択の集合は親から受け取る。** 検索の実行と結果の表示は
> 13-E が持つ（`SearchForm` が条件を親に渡すだけなのと同じ作り）。

> **該当が無いカテゴリの見出しは出さない。** 空の見出しが並ぶと探しにくい。

> **「選択をすべて外す」を用意した。** 1つずつ外させると選び直しのたびに手間が増える。

#### 13-E: 検索結果のUI `feat/ui-search-by-ingredients`

- [x] 🔴 テスト: 1つも選んでいなければ探せない／選んだ食材を送り候補と不足を並べる
- [x] 🔴 テスト: 不足0はその旨を出す／**不足0でも調味料が要ることを伝える**（14.1）
- [x] 🔴 テスト: 見つからなければ食材を増やすよう促す／候補からレシピへ進める
- [x] 🔴 テスト: 食材を選び直したら前の結果を消す／検索の失敗を知らせる
- [x] 🟢 `SearchByIngredientsPage`・ルーティング（`/from-fridge`）・ヘッダ導線

> **実機で見て初めて分かった不具合を直した。** 食材166種を素直に並べると
> ピッカーだけで縦1300px を超え、「探す」を押しても**結果が画面外**に出る。
> 利用者からは何も起きていないように見える。ピッカーを `max-h-[55vh]` の
> スクロール領域にし、ボタンと結果が常に手の届く位置に来るようにした。
> **単体テストは全て緑のままだったので、これはスクリーンショットでしか気づけない。**

> **選び直したら前の結果を消す。** 残したままだと、いま選んでいる食材の
> 結果だと誤解させる（週間献立の保存で「引き直したら知らせを消す」としたのと同じ理屈）。

#### 13-F: E2E `feat/e2e-search-by-ingredients`

- [x] 🔴 食材を選ぶ→検索→候補が出る→レシピへ進める（調味料の断りも確認）
- [x] 🔴 選び直すと前の結果が消える／選択をまとめて外せる／未ログインでも使える
- [x] 🔧 README の更新（概要・設計判断・対象外の記述）

> **チェックボックスは label を押す。** 本体は sr-only で label に覆われており、
> input を直接クリックできない（`helpers.ts` の `choose` と同じ理由）。

> **候補の中身は検証しない。** 何が出るかは献立マスタ次第なので、
> 件数と表示の形（「使える食材:」「あと◯品:」）だけを見る。

---

## フェーズ14: 献立の役割（主菜／副菜／汁物）

> 利用者レビューの項目 B。**動機は引き直しコスト。**
> 「今日の晩御飯を選ぼうとしたのにカプレーゼやコーンスープが出てくると、
> それ単品では夕食として成立しないので必ず引き直しになる」。
> 加えて「お弁当を作る人は主菜より副菜を多めに出してほしい」。
>
> 仕様は spec.md 2.10 / 2.1 / 2.2 / 4.2 / 5.1。マイグレーション番号は **000009 を予約**。
>
> ブランチ: `feature/menu-role`。

### 着手前に決めたこと（2026-07-23）

3点ともユーザーの判断を仰いだ。

| 論点 | 決定 | 理由 |
| --- | --- | --- |
| スコープ | **絞り込みの軸を1本足すだけ**。組では返さない | 組にすると履歴・お気に入り・週間献立の保存・買い物リストの「1件＝1献立」が全て崩れる。引き直しコストの解消には絞り込みで足りる |
| 役割の値 | **3値 `main` / `side` / `soup`** | 汁物を分けると「主菜＋副菜＋汁物」の定番の組が後から作れる。2値だとスープが2品並ぶ |
| 副菜の不足 | **同フェーズでマスタを拡充する** | 分類しただけでは副菜・汁物が40件しかなく、ジャンル×難易度で1桁になる |

**未指定の既定は `main`。** ジャンル・難易度の未指定は「すべて」だが、役割だけ意味を変える。
未指定のときに最も起きてほしくないのが副菜の単品提案なので安全側に倒す。`all` を明示すれば全部から引ける。

**丼・麺・鍋・定食は `main`。** 単品で夕食が成立するかを基準にする。

> **実データの確認（2026-07-23）。** マスタ360件を通しで読んだ内訳:
> 副菜にあたるもの約27件、汁物約13件、残り約320件が主菜（丼29・麺31・定食5を含む）。
> **副菜・汁物は全体の11%しかない。** ジャンル×難易度は12通りあるので、
> 分類だけでは1マスあたり3件程度にしかならず、`side` を選べるようにしても
> 同じ献立が何度も出る。週間献立の重複回避（7日分）は成立しない。

#### 14-A: 仕様 `docs/menu-role-spec`

- [x] 🔧 spec.md 2.10（新設）・2.1 / 2.2（役割の既定）・1.2（スコープ）・4.2（`role` 列）・
      5.1（`role` パラメータと応答）・10章（フェーズ12〜14の行を追加）
- [x] 🔧 task.md にフェーズ14の枠。利用者レビューの項目 B に着手を記録

#### 14-B: 役割の列と分類 `feat/menu-role-schema`

- [x] 🔴 テスト: `Role` のパース（3値・未知の値・空文字・`all` を弾く）／
      `Menu` の検証に `role` が入る／シードの役割ごとの件数と付け漏れ／
      役割の付け替えが ON CONFLICT で既存行に反映される
- [x] 🟢 マイグレーション `000009_add_menu_role`（`NOT NULL DEFAULT 'main'` + CHECK + 索引）
- [x] 🟢 `domain.Role`、`Menu.Role`、repository の scan / columns
- [x] 🔴 テスト: 履歴・お気に入り・保存した週間献立の各経路で `Role` が復元される
- [x] 🟢 `joinedMenuColumns` に集約（履歴・お気に入り・保存した週間献立の3経路）
- [x] 🟢 `ErrInvalidRole` の problem 写像（400 `invalid-role`）
- [x] 🔧 既存360件への役割付与（seeds/menus.sql）。**main 315 / side 30 / soup 15**

> **`ParseRole` は `all` を弾く。** APIの絞り込みで使う `all` は「絞り込まない」ことを
> 示す値で、献立が持つ役割ではない（spec.md 5.1）。ここで通すと CHECK 制約と食い違う。

> **DEFAULT を `main` にしたのは付け替えを減らすため。** 360件中314件が主菜なので、
> 既存行を `main` で埋めてから副菜・汁物46件だけをシードで直すほうが差分が小さい。

> **`role` を ON CONFLICT の更新対象に含めた。** 分類は主観が入り後から見直す前提なので、
> 含めないとシード済みのDB（本番）では初回の分類のまま固定されてしまう。

> **JOIN 経路3つが取り残されていた（コードレビューで発覚）。** 履歴・お気に入り・
> 保存した週間献立は `menus` を独自に JOIN しており、献立の列リストを**3箇所にコピー**していた。
> `repository/menu.go` だけ `role` を足したため、この3経路は `Menu.Role` が空のまま返っていた。
> **静かに通っていた**理由は3つ: ①`menuDTO` がまだ `role` を出さない ②読み取り経路で
> `Menu.Validate()` を呼ばない ③SELECT と Scan の列数は揃うのでテストが落ちない。
> `joinedMenuColumns` に集約し、3経路それぞれに `Role` の検証を足した。
> 検証は `side` で行う（`main` だと DBの DEFAULT と区別が付かない）。

> **写像漏れ検査が `ErrInvalidRole` を捕まえた。** #129 で入れた仕組みで、
> **同型の見落としはこれで3度目**（フェーズ12の保存ID・13の食材ID・今回の役割）。
> 今回は検査があったため handler に届く前に気づけている。
> 14-D で `role` クエリを解釈するようになると、無いと壊れた値が 500 になっていた。

#### 14-C: 副菜・汁物の拡充 `feat/menu-side-expansion`

- [x] 🔧 `side` を各ジャンル10件以上、`soup` を各ジャンル5件以上（合計60件以上）。
      **難易度は跨いで数える**。**実績 +19件**（side 30→42 / soup 15→22）
- [x] 🔧 和食×手が込んだ の主菜を1件補充（土瓶蒸しを汁物にして9件に落ちたため）
- [x] 🔴 テスト: 役割ごとの実数／`(genre, role)` の下限／
      `(genre, difficulty)` ごとの主菜の実数と下限10件

> **`(genre, difficulty, role)` の全マスで10件は満たさない。** それを保証すると
> 副菜・汁物だけで 4ジャンル × 3難易度 × 2役割 × 10 = 240件が要り、
> 現状40件からの追加が現実的でない。**そもそも「手の込んだ副菜」の需要が薄い。**
> 難易度まで絞ったときの枯渇は 2.2 の既存の緩和ルールで吸収する。
- [x] 🔧 追加分の `menu_ingredients` も揃える（買い物リストと食材検索が欠けないよう）

> **目安として書いた「+40〜60件」は過大だった。実際の不足は +17件。**
> ジャンル別に数えると和食の副菜10件・洋食と中華の汁物6件は既に足りており、
> 不足は「和食の汁物」「その他の汁物」「洋食・中華・その他の副菜」に偏っていた。
> 薄いマスに余裕を持たせて +19件にしている。

> **「各ジャンル40件」は主菜の件数ではなかった。** 2026-07-22 に easy / normal を
> 各40件に増やしたとき、その40件には後に副菜・汁物に分類されるものが混ざっていた。
> 役割で数え直すと主菜は 31〜37 件（例: 洋食×簡単は40件中9件が副菜・汁物）。
> **件数を減らしたのではなく内訳が見えただけ**だが、テストの期待値は実数に直した。

> **⚠️ 14-C 単体では体験が悪化する。14-D まで揃えて初めて改善になる。**
> 絞り込みはまだ入っていないので、既定の検索は380件すべてから引く。
> 副菜・汁物の比率が 45/360（12.5%）から 64/380（16.8%）に上がり、
> **「晩ごはんにカプレーゼが出る」確率はこの時点では上がっている。**
> 本番に届かないのは `feature/menu-role` を `main` に出していないからで、
> **この順序はブランチ構成だけが担保している**（コード側の歯止めは無い）。
> `feature` を `main` に出すのは 14-F まで完了してから。途中で出さない。

> **和食×手が込んだ の主菜が9件に落ちていた。** 土瓶蒸しを汁物に分類した結果で、
> シードが掲げる「(genre, difficulty) ごとに最低10件」を割っていた。
> 治部煮を足して戻し、**実数の表とは別に下限の検査も入れた**。
> 実数だけだと、目減りしても「表を書き換えれば通る」で見逃される。

#### 14-D: 絞り込みAPI `feat/api-filter-by-role`

- [x] 🔴 テスト: 既定が `main`／`all` で全役割／`side` で副菜のみ／未知の値は 400／
      週間献立と引き直しにも効く／応答に `role` が入る
- [x] 🟢 `domain.ParseRoleFilter`、`MenuFilter.Role`、repository の WHERE、
      handler のパース（`GET /menus/suggest`・`POST /menus/suggest-weekly`・`POST /menus/reroll-day`）
- [x] 🔧 `api/openapi.yaml` に `Role` / `RoleFilter` / `RoleQuery` を追加し `make gen-api`

> **既定を入れるのは入力を解釈する層（`ParseRoleFilter`）。** `MenuFilter.Role` の nil は
> 「絞り込まない」であって「既定」ではない。両方の意味を持たせると、
> repository が「nil のときは main」を知る必要が出てレイヤが混ざる。

> **`Role` と `RoleFilter` をスキーマとして分けた。** 献立が持つのは3値だが、
> クエリは `all` を含む4値。1つにすると `all` が保存できる値に見えてしまう。

> **契約テストが `role: ""` を捕まえた。** handler のテストが作る `domain.Menu` に
> 役割を入れておらず、enum に一致しない値が応答に出ていた。
> OpenAPI と実際の応答を突き合わせる仕組み（9-0）が働いた形。

#### 14-E: UI `feat/ui-role-filter`

- [x] 🔴 テスト: 「種類」に3種＋すべてが出る／既定が主菜／選択が検索条件に乗る／
      「すべて」は `all` を送る（undefined ではない）
- [x] 🔴 テスト: 結果カードに役割が出る（`MenuCard.test.tsx` を新設）
- [x] 🟢 `SearchForm` / `MenuCard` / `filter.ts`（条件の型と既定を切り出し）
- [x] 🔧 実機で確認（検索・週間献立の両方、スクリーンショット）

> **「すべて」は `all` を明示して送る。** 省略するとサーバ側で主菜になるため
> （spec.md 2.10）、undefined にすると「すべて」を選んだのに主菜に絞られる。
> ジャンル・難易度は未指定＝すべてなので省略でよく、ここだけ扱いが逆になる。

> **選択肢の並びは呼び出し側が決める形に変えた。** `RadioGroup` が「すべて」を
> 先頭に固定していたが、役割は既定が主菜で「すべて」は末尾に来る。
> 並びで既定が読み取れるようにするため、値の組を渡す形に一般化した。

> **`sessionStorage` に古い形が残る問題を塞いだ。** 週間献立の絞り込み条件は
> セッションに持つため、役割を足す前にタブを開いていた利用者は
> `{genre, difficulty}` だけを持っている。そのまま送ると `role=undefined` という
> 文字列になって 400 になる。`withDefaultRole` で補う。

> **役割バッジはジャンル・難易度と同じ見た目にした（レビュー指摘）。**
> 当初 `bg-kon-leaf/20 font-medium` で強調していたが、この配色は
> ナビの現在地（`App.tsx`）と選択中のラジオ（`SearchForm` / `IngredientPicker`）に
> 使っている「選択中」の色で、静的なタグに流用すると意味が二重になる。
> 強調は「先頭に置く」という位置で足りる。

> **お気に入り一覧にも役割を出した（レビュー指摘）。** `MenuCard` を通らず
> 独自にタグを並べている画面があり、そこだけ役割が出ないと
> 詳細を開くまで何の一品か分からない。「すべて」で引けば副菜も登録できるため実際に起きる。
> **履歴は対象外**（ジャンル・難易度も出しておらず、日時と検索種別だけの画面のため）。

> **条件の型と定数は `filter.ts` に切り出した。** `SearchForm.tsx` に置くと
> コンポーネント以外を同居させることになり、Fast Refresh が効かなくなる
> （oxlint が警告する）。フォームとAPI呼び出しの両方が使うものなので独立させた。

#### 14-F: E2E `feat/e2e-menu-role`

- [x] 🔴 既定で引くと主菜が出る／種類の既定が主菜で選択されている／
      副菜・汁物に切り替わる／**引き直しても選んだ種類のまま**／
      週間献立の既定は7日とも主菜／未ログインでも使える（計7本）
- [x] 🔧 README の更新（概要・設計判断・対象外の記述）

> **副菜を出したとき「主菜」のバッジが無いことも見る。** 役割が出るだけでは
> 取り違えを検出できない。カードに両方出てしまうと絞り込みの意味が消える。

> **引き直しを3回繰り返して条件が保たれることを見る。** 引き直しは条件を
> 使い回す経路で、ここが抜けると主菜に戻る。単発の検索だけでは気づけない。

> **役割の照合は `exact: true` にする（レビュー指摘）。** 部分一致だと
> **説明文まで拾う**。カードは役割バッジと説明を同じ `article` に描くため、
> 「これだけで主菜になる」（コブサラダ）や「毎日の汁物の基本形」
> （豆腐とわかめの味噌汁）が引かれると2件一致して strict mode violation になる。
> 汁物は母数22件なので**4.5%の確率で落ちる**計算だった。
> 該当の献立を固定して再現し、`exact: true` で1件になることを確認している。

> **「すべて」は「探せること」だけ見る。** 何の役割が出るかは検証しない。
> 主菜が83%を占めるため「副菜が出ること」を待つと確率的なテストになる
> （13-F の「候補の中身は検証しない」と同じ理由）。
> 絞り込みが外れること自体は repository / handler の単体テストが持つ。

> **選択の永続化はしない（対象外）。** 画面を離れて戻ると種類は主菜に戻るが、
> ジャンル・難易度も同じ挙動で、spec.md 2.10 も永続化を求めていない。
> 週間献立（2.8）だけが sessionStorage を使うのは、組み立て中の週を失わせないため。

---

## 横断的な改善

### エラーの写像漏れを検査する `test/problem-mapping-coverage` ✅

- [x] 🔴 テスト: `domain` / `service` / `auth` の公開エラーが全て problem.go に写像されているか、
      意図的な除外リストに理由つきで載っている
- [x] 🔴 テスト: 除外リストに、もう存在しないエラーが残っていない
- [x] 🔧 写像を1つ外して落ちることを確認（2ケースで検証）

> **同じ見落としを2度やったので構造で塞いだ。** domain にエラーを定義しても
> problem.go の写像を書き忘れると、handler まで届いた瞬間に 500 になる。
> フェーズ12（保存ID）・フェーズ13（食材ID）で同型の漏れが出た。
> どちらも「その経路を通るテストを書いて初めて気づく」性質で、
> 定義しただけの段階では誰も気づけない。
>
> **実行時には取れないので構文解析で見る。** Go はパッケージ変数を名前から引けない。
> `problem.go` の写像表と、各パッケージの `Err` で始まる公開変数を突き合わせる。
>
> **調査の結果、main 時点で実害のある漏れは無かった**（未写像10件はすべて
> 「HTTP応答にしない」もの）。除外リストには理由を必ず書く決まりにしている。
> 理由が書けないなら、それは写像すべきエラーである可能性が高い。
>
> **`parser.ParseDir` は使わない**（Go 1.25 で非推奨）。代替の `go/packages` は
> 依存が増えて型検査まで走るため、`os.ReadDir` + `parser.ParseFile` で自前走査している。

---

## 積み残し / 将来対応

spec.md 1.2 で MVP 対象外としたもの。

- [ ] **アレルギー・苦手食材の除外（食材ブラックリスト）**（2026-07-21 具体化）
      利用者が「食べられない食材」を登録し、それを含む献立を候補から除外して引く。
      ユーザー単位の設定なので保存先が要る（`users` に紐づくテーブル）。
      候補が枯渇したときの挙動を決めること（2.2 の重複回避ルールと同様に緩和するのか、
      0件と返すのか）。
- [x] ~~買い物リスト生成~~ → **フェーズ11 として着手**
- [ ] 朝食・昼食の献立（現在は夕食のみ）
- [ ] 献立のユーザー投稿
- [ ] **栄養素の管理**（2026-07-21 具体化）
      献立ごとの栄養価を出すだけでなく、日／週の単位で摂取量を積み上げて見せる。
      「計算」より広く、履歴（`search_histories`）と結びつけて期間で集計する話になる。
      要決定: 目標値を持つか（性別・年齢別の推奨量を入れるとユーザー属性が必要になる）、
      週間献立の提案時に栄養バランスを考慮するのか、事後の可視化に留めるのか。
- [x] ~~**必要食材の取得・表示**（2026-07-21 追加）~~ → **フェーズ11 として着手**
      レシピリンクと併せて必要食材を出し、複数のレシピページを開かなくても
      買い物リストにできるようにする。上の「買い物リスト生成」の前提にあたる。
- [ ] **週間献立での共通食材の再利用**（2026-07-21 追加）
      1週間分の提案時に、食材が適度に重なるよう候補を選ぶフィルタを設け、
      使い切れずに余る food loss を減らす。「適度に」の度合いは要検討
      （重なりすぎると献立が単調になり、2.2 の重複回避ルールと綱引きになる）。
- [x] ~~**手持ちの食材から献立を検索**（2026-07-21 追加）~~ → **フェーズ13 として着手**
      冷蔵庫にある食材を入力し、それで作れる献立を提案する。ジャンル×難易度による
      現在の検索とは入力の軸が違うため、既存の `/menus/suggest` を拡張するか
      別エンドポイントにするかを決める必要がある。
      「全部揃っている献立だけ」か「大半が揃っていれば可（不足分を買い物リストに回す）」
      かで体験が変わるので、そこも要決定。
      **2026-07-22: 利用者レビューでも同じ要望が出た**（「冷蔵庫にある材料で何が作れるのか
      知れたら最高です」）。着想ではなく実利用からの要望として裏が取れたことになる。
- [x] ~~本番デプロイ（Neon / Cloud Run / Cloudflare Pages）※spec.md 12章~~ → フェーズ10 として着手

> **上記の食材まわり4件（ブラックリスト・必要食材の表示・共通食材の再利用・手持ち食材での検索）は
> 「献立マスタが食材を持つ」ことが共通の前提。** 現状 `menus` に食材の
> カラムは無く、レシピは spec.md 1.1 の判断により**外部検索APIでリンクを得るだけ**で
> 中身をクロールしない（主要レシピサイトが robots.txt でクローラを拒否しているため）。
> つまり食材はレシピページから取れない。着手時はまず「食材データをどこから調達するか」
> （マスタに手入力で持たせる／食材付きのデータソースを探す）を決める必要がある。
>
> **→ 2026-07-21 決着。自前マスタに持たせる（spec.md 14.1）。** レシピAPIは自前の献立名と
> 確実に対応づかず、成分表は「料理→食材」ではないため、外部調達はいずれも成立しなかった。
> フェーズ11 でこのデータを整備するので、**残り3件（ブラックリスト・共通食材の再利用・
> 手持ち食材での検索）のブロッカーもフェーズ11 完了時に解消される。**
>
> **→ 2026-07-22 解消。フェーズ11 完了により `ingredients` / `menu_ingredients` が入った。**
> ブラックリスト・共通食材の再利用・手持ち食材での検索は、いずれも着手可能になっている。
>
> **「栄養素の管理」はさらに一段深い依存を持つ。** 食材名が分かっても栄養価は出ないため、
> 食材→栄養価の対応表（日本食品標準成分表など）と、各献立の**分量**データが要る。
> 上の4件は分量抜きの食材名リストでも成立するが、栄養価計算だけは分量が無いと数字が出ない。
> 着手順としては最後に置くのが素直。

---

## 利用者レビューからの後続タスク（2026-07-22）

本番公開後、複数の利用者から届いた感想をタスク化したもの。
**出所が実利用のため、ここでは「何を言われたか」を先に残し、解釈と設計判断を分けて書く。**

### 全体に効く前提: 「珍しい料理」は欠点でもあり長所でもある

同じ献立マスタに対して、正反対の声が同時に届いた。

- 「フカヒレの姿煮・鴨料理など、そもそも高額でスーパーで買えないものが出てくる。
  それと対比して『ポテトサラダ』が出るのに違和感」「自分はフカヒレを買えません」
- 「珍しい料理が出てくるので、そういう楽しみ方もできますね！」

**したがって「変な献立を消す」方向で解いてはいけない。** 珍しい料理を引ける楽しさは
現に評価されている性質であり、消すと別の利用者の価値を壊す。
出し分けの軸（下の A / C）を足して、利用者が選べる形にするのが筋。

### 維持すべき性質（変更時に壊さないこと）

レビューで明示的に褒められた点。今後の変更でこれらを損なう場合は理由を書くこと。

- マスコット（こんたてん）のかわいさ
- 1週間の献立から買い物リストが出ること（フェーズ11）
- **「引き直す」という語感**（「クスッときました」）。無味乾燥な「再検索」等に変えない
- 珍しい料理が出ること（上記）

---

### A. ターゲット層の明確化とモード選択

> 「運用していくならターゲット層を明確にした方がいい。子供のいる家庭用とか1人暮らし用とか
> （モードとして選択できてもいいかな）」

現在の献立マスタ360件は想定利用者を決めずに作っている。**これは機能追加である前に
プロダクトの位置づけの問題**で、C（入手性）・D（季節）・H（予算）の出し分け基準は
すべてここに依存する。先に決めないと、個別のフィルタが場当たりになる。

要決定:
- モードを「利用者の属性」（1人暮らし / 子どもあり）で持つか、「その時の気分」
  （手軽に / 本格的に）で持つか。前者はユーザー設定、後者は検索条件になり保存先が変わる
- モードが変えるのは何か（候補の絞り込み / 並び順 / 分量表示 / 難易度の既定値）
- 既存の履歴・お気に入りとの関係（モードを変えたら過去の履歴はどう見えるか）

### B. 献立の粒度をメイン／副菜で分ける 〈2026-07-23 フェーズ14として着手〉

> 「想定している献立メニューが主役料理なのか、サブも含めるのかですが、
> メインとサブでジャンル分けしてもいいと思いました」

**現在のマスタは粒度が混在している。** 実データで確認した例:

| 献立 | 粒度 |
| --- | --- |
| フカヒレの姿煮 | 主菜1品 |
| ポテトサラダ | 副菜 |
| おせち料理 | 献立全体（多数の品） |

「フカヒレとポテサラが並ぶ違和感」の一因はここにある。値段だけの問題ではなく、
**同じ列に別の粒度のものが並んでいる**ため比較が成立していない。

`menus` に役割（主菜 / 副菜 / 汁物 …）の列を足すのが素直。
ただし「1食＝主菜1品」という現在の前提（2.1）を「主菜＋副菜の組」に変えるなら、
API の応答形と週間献立の重複回避（2.2）まで波及する。**影響範囲が最も広い項目。**

#### 2026-07-23: フェーズ14として着手。組にはせず絞り込みに留める

利用者から動機が具体化された。**「引き直しコスト」が本体。**

> 「今日の晩御飯を選ぼうとしたのにカプレーゼが出てくるとか、コーンスープが出てくると、
> それ晩御飯として成り立たないから引き直しをしなきゃいけない」
> 「お弁当を作る人だと主菜よりも副菜を多めに出してほしいというタイプの人もいる」

**「影響範囲が最も広い」を回避する形で着手できることが分かった。**
上の懸念は「1食＝主菜＋副菜の組」にした場合のもので、
**役割を絞り込みの軸として足すだけなら応答形は変わらない。**
履歴・お気に入り・週間献立の保存・買い物リストの「1件＝1献立」という単位が保たれるため、
波及は `menus` への列追加とフィルタ1本に収まる。組で返す形は将来の選択肢として残す。

**代わりにマスタ拡充が前提になった。** 副菜・汁物は360件中40件しかなく、
分類だけでは `side` を選んでも枯れる。詳細はフェーズ14を参照。

### C. 日常性（庶民的かどうか）を難易度から分離する 〈最優先〉

> 「フカヒレの姿煮から、鴨の料理とか、そもそも高額、スーパーで買えないやつが出てくる」

`menus.difficulty` は `easy` / `normal` / `elaborate` の3値で、
フカヒレの姿煮・佛跳牆・北京ダック・コンフィ・ド・カナール・おせち料理は
いずれも `elaborate`。**つまり現状は「手間がかかる」と「高価・入手困難」を
1つの軸に同居させている。** この2つは本来独立で、
「手間はかかるが材料は普通」（煮込みハンバーグ）と
「手順は単純だが材料が手に入らない」（フカヒレ）を区別できない。

#### 2026-07-22: 最優先に格上げ。H（予算）をここに吸収する

> 「予算内でっていうのは本当に厳しいと思う。**庶民的な料理とそうでないものとかを
> 分けることができれば予算問題は大きく解決しそう。**
> 現状、親子丼は簡単で安価だが、北京ダックとかどうつくんねんっていうw」

**軸の名前は「価格帯」ではなく「日常性（庶民的かどうか）」が正しい。**
北京ダックは高いだけでなく、**そもそも家庭で作るものではない**。
価格・入手性・家庭で作れるか、は実用上まとめて1本の軸になる。

`elaborate` 40件を実際に見ると、3つが混ざっている:

| 実体 | 例 | 件数の目安 |
| --- | --- | --- |
| 手間はかかるが日常の料理 | ビーフシチュー・ラザニア・すき焼き・パエリア・火鍋・ボルシチ | 約14 |
| ハレの日。作れるが材料が高い/入手しにくい | ローストビーフ・鴨鍋・鰻の蒲焼き・ブイヤベース・小籠包 | 約18 |
| 家庭で作るものではない（外食・専門店の領域） | 北京ダック・フカヒレの姿煮・佛跳牆・握り寿司・上海蟹の紹興酒漬け・ビーフウェリントン | 約8 |

**難易度は「手間」の軸として作られたもので、日常性はそれと直交する別の軸。**
`elaborate` の中で3つに割れている。

> **2026-07-22 追記: マスタが360件に増えた（PR #118）。** 内訳は
> `easy` 160 / `normal` 160 / **`elaborate` は40件のまま**。当初この分析を書いた時点では
> `elaborate` はマスタの1/3だったが、**今は約11%まで薄まっている。**
>
> **薄まっただけで解決はしていない。** 「手の込んだ」を選べば依然としてフカヒレの姿煮が出るし、
> 条件を絞らない週間献立でも約11%の確率で混ざる。ただし**優先度は当初想定より下がる**。
> 上の3分類（日常の手間／ハレの日／家庭で作らない）は elaborate 40件が
> 変わっていないためそのまま使える。
>
> 一方で **`easy` / `normal` の320件を通しで確認する手間は4倍**になった。
> 日常性の付与は elaborate 40件から先に手を付け、残りは既定値 `everyday` を置いて
> 例外だけ拾う進め方が現実的。

提案する列（`menus` に追加、3値）:

- `everyday` … 日常の食卓。材料がスーパーで揃い、価格も普通（親子丼）
- `special` … ハレの日。作れるが材料が高い、または手に入りにくい（ローストビーフ・鴨鍋）
- `showcase` … 家庭で作る前提がない。外食・専門店の領域（北京ダック・フカヒレの姿煮）

**既定では `showcase` を出さない。ただし削除も除外もしない**（明示的に選べば出る）。
「珍しい料理が出るのが楽しい」という評価を壊さないため、ここは必ず選択式にする。

これで**実額を持たずに予算の動機（使いすぎたくない）をかなり満たせる**。
実際の金額計算はやらない（理由は H）。

### D. 季節・旬

> 「月に合わせて旬の食材を使ったやつとか、春夏秋冬で献立を決めてもいいかも
> （おせちとか出てきた、、、困った）」

7月に「おせち料理」が出るのは、`menus` が季節属性を持たないため。

段階が2つある。**浅い方（献立に季節タグを付け、時期外れを出さない）だけでも
「おせち問題」は解ける**ので、まずはそこまでで切るのが現実的。

1. 献立に季節タグ（通年 / 春夏秋冬 or 月）を持たせ、時期外れを候補から外す
2. 食材の旬を持たせ、「旬の食材を使った献立」を積極的に推す

要決定: 季節外れを**完全に除外**するか、**優先度を下げる**だけにするか。
完全除外は上の「珍しい料理が出る楽しさ」を削るため、既定は優先度下げが無難。

### E. 献立の画像

> 「献立を出す時に画像あるといいと思いました」

**調達が最大の課題で、実装量より先に決めることがある。** レシピサイトの画像は
1.1 / 14.1 と同じ理由で使えない（クロール不可・自前の献立名と対応づかない）。

選択肢:
- 商用利用可の素材（写真AC等）から**360件分**を手作業で紐付ける。ライセンス条件の確認が要る。
  **マスタが360件に増えた（PR #118）ぶん、当初見積もりの3倍の手間になった。**
  この項目の実現性は目に見えて下がっており、着手順を最後に置く根拠が強まっている
- **こんたてんのイラストで代替する**。写真の説得力は無いが、権利が完全にクリアで、
  マスコット基調のデザイン（PR #101）とも噛み合う
- 生成AIで用意する。権利と品質の両面で要検討

配信は Cloudflare Pages の静的配信に置ける（マスコット素材と同じ扱い）。

### F. 週間献立をまとめて保存する 〈2026-07-22 フェーズ12として着手〉

> 「一週間の献立を丸ごと保存できたら、いざ買い物に行ったときにすぐに見ることができて嬉しい」

**現状は sessionStorage に持っているだけ**（Issue #83）。タブを閉じると消え、
スマホで買い物中に見ることができない。お気に入りは献立1件単位なので、
**週セットとしての保存は別物**。

サーバ保存（`users` に紐づく週間献立テーブル）が要る。要決定:
- 保存できる件数（履歴のような FIFO 上限を設けるか）
- 買い物リストとの関係（保存した週から常に買い物リストを再生成するか、リストも固定するか）

**レビューで最も具体的に「嬉しい」と言われた項目で、依存も浅い。着手候補として筆頭。**

### G. 週間献立で日付を前後させる 〈2026-07-22 やらないと判断〉

> 「多分一週間の献立の画面で引き直したり、メニューの日付を前後させて（できたら嬉しい）
> 献立を固めてってなると思うんですよね」

**日ごとの引き直しは実装済み**（`WeeklyPage.tsx`）。レビューはその先の話で、
「木曜のこれを土曜に動かしたい」という**並べ替え**が無い。

引き直しと違って外部への問い合わせが不要で、クライアント側の並べ替えで済む。

**→ 2026-07-22 やらない。** 実装は軽いが、**用途が実在しない**と判断した。
献立に曜日固有の意味が無いため（平日/休日で作れる手間が違う、という設計にはなっていない）、
「この日のこれを別の日に動かしたい」という動機が成立しにくい。
気に入らない日は引き直せば済み、並べ替えでしか解けない要求が見当たらない。

やるとしたら B（主菜/副菜）や A（モード）で曜日ごとの制約が入り、
「金曜は手の込んだものにしたい」のような意味が曜日に付いてからになる。
**その時が来たら再検討する。それまでは着手しない。**

### H. 1週間の予算内で献立を決める 〈2026-07-22 C に吸収。実額はやらない〉

> 「1週間の予算を決めてその金額内で食材を決めて出してくれる機能とかあるといいかも
> （できるかわかりませんが実際のスーパーの金額をもとに算出）」

**要望の中で最も依存が深く、実現性が低い。** 理由:

- **価格データの調達手段が無い。** 地域差・季節変動・店舗差があり、
  公開された使えるデータソースが見当たらない。14.1 で外部調達を検討した時と同じ壁
- **分量を持たないと決めている**（14.2）。単価が分かっても「玉ねぎ1/2個」が
  分からなければ金額にならない。栄養素の管理と同じ前提を必要とする

**→ 2026-07-22 決着。実額の算出はやらない。C（日常性の分類）に吸収する。**

> 「予算内でっていうのは本当に厳しいと思う。庶民的な料理とそうでないものとかを
> 分けることができれば予算問題は大きく解決しそう」

要望を出した側とこちらの見立てが一致した。**利用者の動機は「1円単位で管理したい」ではなく
「高すぎるものが混ざってくると困る」**なので、日常性の3値で `everyday` に寄せられれば満たせる。

実額をやるなら価格データの調達と分量データの整備が前提になるが、
その労力に見合う追加価値が上の一致で消えた。**この項目は C の完了をもって閉じる。**
再開するとしたら、C を入れてなお「金額が見たい」という声が出たときに限る。

---

### 依存関係のまとめ

```
C (日常性: everyday / special / showcase)   ← 最優先。H を吸収して閉じる
     ↑ 既定の出し分けは A の結論に従う（A 未決でも列の追加とデータ付与は先行できる）

A (ターゲット層・モード) ─┬─→ D (季節)
                          └─→ C の既定値

B (メイン/副菜)  … 2026-07-23 フェーズ14として着手。絞り込みに留めたので波及は小さい
     ↓ 副菜・汁物のマスタ拡充を伴う（C のマスタ作業と競合しうる）

F (週間献立の保存)  … 依存なし。単独で着手できる

E (画像)  … 素材の調達方針を決めるまで着手できない

G (日付の並べ替え)  … やらない
H (予算・実額)      … やらない（C に吸収）
```

**当初の着手順の提案: C → F → A → D → B → E。**
C を先頭に上げたのは、`elaborate` 40件に3種類が混在している現状が
「フカヒレ問題」の直接の原因であり、**列を足してデータを付与するだけなら
A の結論を待たずに進められる**ため。既定でどれを出すかの判断だけが A に依存する。
F+G は依存が無く効果が具体的。A は後続の判断基準になるので早めに決める。
B と H は影響範囲・前提の重さから後ろに置く。

> **2026-07-23 に順序を変更した。F の次は C ではなく B（フェーズ14）。**
> 利用者から B の動機が「引き直しコスト」だと具体化され、
> **組にせず絞り込みに留めれば影響範囲が小さい**ことが分かったため。
> C は依然として残っており、B の次に着手する。
>
> **B と C はどちらも `db/seeds/menus.sql` を触る。** フェーズ12で C を後回しにしたのと
> 同じ衝突が起きるので、**並行させず B を出し切ってから C に入る。**
> B で副菜・汁物を +40〜60件足すため、C の分類対象もその分増える。
